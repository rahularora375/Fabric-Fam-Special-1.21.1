# SYSTEMS.md

Non-item architecture: subsystem wiring, components, status effects, sounds, item-group helpers, armor tick effects, attack handlers, Necromancer summon, loot injection, client-side overlays, and networking. Root context: `CLAUDE.md`. For per-theme item content that drives these systems, see `ITEMS.md`. For mixin hooks, see `MIXINS.md`.

Loot-table injection is programmatic — no datapack loot JSONs. Datapack assets in use: one structure tag (`data/famspecial/tags/worldgen/structure/on_ancient_city_maps.json`) backing the Ancient City explorer map destination, plus a small set of enchantment tag overrides driving Mjolnir's enchant eligibility (see "Datapack tag overrides" below).

## Subsystem wiring

Fifteen subsystems registered from `FamSpecial.onInitialize()` in this order: components → status effects → sounds → **ThorItems registration** → **RaidersLegacyItems registration** → item groups → loot modifier → armor effects → attack handlers → block-break handler → Bounty Hunter kills → Necromancer summon → ThorEffects → Fortune & Glory tick handler → version handshake. `ThorItems.register()` and `RaidersLegacyItems.register()` both run **before** `ModItemGroups.registerItemGroups()` because registering an `Item` subclass (Mjolnir is `MjolnirMaceItem`, Fortune & Glory is `FortuneGloryItem`, neither is a vanilla `Item` instance) at static-final class-load crashes in 1.21+ — `SimpleRegistry` rejects intrusive holders during the early-boot window the creative-tab-entries lambda runs in. Additionally, `client/FamSpecialClient` is a **separate `client` entrypoint** declared in `fabric.mod.json`; it runs independently of `onInitialize` and only on the physical client.

**Both-sides install requirement.** The mod must be present on both client and server in multiplayer. `net/VersionHandshake.java` runs a login-phase query on the `famspecial:version` channel: the server sends its `MOD_VERSION` string to every connecting client in `ServerLoginConnectionEvents.QUERY_START`; the client replies with its own version via `ClientLoginNetworking.registerGlobalReceiver`. Mismatch policy is strict equality — any difference, or a client that doesn't understand the query (vanilla / no-mod client), triggers `ServerLoginNetworkHandler.disconnect(...)` with a human-readable message naming both versions. `MOD_VERSION` is read once at class-load from `FabricLoader.getInstance().getModContainer(MOD_ID)` so it always matches the jar's `fabric.mod.json` (populated via `${version}` → `gradle.properties` — see the Commands section for how to bump it for a release). Kicked clients never reach the play state, so there's no partial-state window where loot injection or attack handlers could run for a mismatched peer.

## `component/ModComponents.java`

Registers the custom `DataComponentType`s used to flag behavior on `ItemStack`s (the 1.20.5+ mechanism for attaching state/flags to stacks):

- `famspecial:grants_night_vision` (`Boolean`) — helmet flag driving Night Vision while worn.
- `famspecial:set_id` (`String`) — marks a piece as belonging to a named armor set (currently `"mistborn"`, `"pacifist"`, `"poseidon"`, `"fire_serpent"`, `"necromancer"`, `"knight_radiant"`, `"esh_endra_navesh"`, `"shurima"`, `"thor"`, or `"raider"`). Four equipped pieces with matching `SET_ID` trigger that set's bonus (in `ArmorEffects.BONUSES` for status effects, or in `TridentItemMixin` for the Poseidon riptide override). `"thor"` is the only 5/5 set — Mjolnir counts as the 5th piece via its `THOR_MACE` flag, checked separately from the armor-slot `SET_ID` iteration.
- `famspecial:is_famspecial_gear` (`Boolean`) — generic identity marker stamped on every themed stack produced by `ModItemGroups.buildItem`. Covers all 11 themes (including OldFam, whose pieces don't carry `SET_ID`). Acts as the **NBT-forgery gate** on `AnvilScreenHandlerMixin`'s chestplate→elytra merge: a stack whose `SET_ID` is in the merge allowlist but whose `IS_FAMSPECIAL_GEAR` is missing or not `true` is rejected, so a plain diamond chestplate with a hand-stamped `SET_ID` cannot abuse the merge path. Carried across the merge via `copyModComponentsForElytra`, so the resulting elytra remains flagged.
- `famspecial:night_strength` (`Boolean`) — main-hand-only flag driving the Atium Dagger's day/night `AttributeModifiersComponent` swap.
- `famspecial:heals_target` (`Boolean`) — main-hand-only flag making attacks heal the target instead of damaging (Sage's Grace). Handled in `AttackHandlers`. **Does not gate anvil behavior** — Sage's Grace is anvil-modifiable per vanilla rules; the earlier `AnvilScreenHandlerMixin` TAIL guard that blanked outputs on `HEALS_TARGET` items has been removed.
- `famspecial:grants_water_breathing` (`Boolean`) — helmet flag driving Water Breathing while worn (Lorelai's Crown).
- `famspecial:grants_messmers_flame` (`Boolean`) — helmet flag driving Messmer's Flame Aegis (80% fire damage reduction via `LivingEntityMixin`) while worn. Lives on Impaler's Crown; decoupled from the Fire Serpent 4/4 gate so the flame resist can be worn a la carte.
- `famspecial:shows_entity_hp` (`Boolean`) — helmet flag (Sage's Crown) enabling client-side floating HP bars above every remote player in view. Client-only effect, no server behavior; handled in `client/HealthOverlay.java`.
- `famspecial:no_lunge_hunger` (`Boolean`) — weapon flag that suppresses the Lunge enchant's exhaustion cost. Lives on Fire Serpent's Wrath; checked in `ApplyExhaustionEnchantmentEffectMixin` against the enchant context's stack.
- `famspecial:indestructible` (`Boolean`) — elytra-style durability cap + "broken" gate. Enforced in three mixins: `ItemStackMixin` clamps damage at `maxDamage - 1` on the core `damage(...)` overload, plays `ENTITY_ITEM_BREAK` once on the not-broken → broken transition, and calls `player.equipStack(slot, stack)` to refresh the entity's attribute cache. `ItemStackAttributeMixin` HEAD-cancels `applyAttributeModifiers` while the stack is at the cap so the broken piece contributes no armor/toughness/custom attributes. `ItemStackEnchantmentsMixin` returns `ItemEnchantmentsComponent.DEFAULT` from `getEnchantments()` while broken so no enchant benefits (or tooltip entries) apply. `REGENS_DURABILITY` reads bypass both gates (they hit `DataComponentMap` directly), so Stormlight + the 243-tick regen keep firing — regen heals the piece back out of the broken state, at which point `ArmorEffects`' regen block re-invokes `equipStack` to refresh the attribute cache. Lives on the 4 Shard armor pieces.
- `famspecial:regens_durability` (`Boolean`) — passive durability regen flag. Driven by an extra 243-tick cadence block in `ArmorEffects.onEndServerTick` — one durability point restored per slot per 243 ticks while worn. Lives on the 4 Shard armor pieces.
- `famspecial:blocks_mending` (`Boolean`) — **Currently unused; kept for potential future use.** Historically enforced by an anvil TAIL guard (blanking outputs when the sacrifice carried Mending) and an enchanting-table filter (`EnchantmentHelperMixin.getPossibleEntries`), both of which have been removed. No consumer reads the flag today; it's still stamped on the 4 Shard armor pieces and still copied by `copyModComponentsForElytra` across the chestplate→elytra merge so its presence on merged elytras is preserved for forward compatibility.
- `famspecial:grants_shardbearing` (`Boolean`) — main-hand weapon flag granting **Shardbearing** (+10% of the target's max HP as bonus damage on the wearer's melee hits, applied after all mitigation) while held. Lives on Oathbringer. Two consumers read this flag off the attacker's mainhand stack: `LivingEntityMixin`'s `modifyAppliedDamage` ModifyReturnValue adds the max-HP chip to the final damage; `ArmorEffects`' `shardbearing_mainhand` bonus applies the cosmetic Shardbearing HUD badge. Effect and mixin share one gate so they can't drift.
- `famspecial:grants_undead_resistance` (`Boolean`) — chestplate flag driving 60% damage reduction from undead attackers (melee + ranged). Lives on Smooth Criminal's Vestment (Necromancer chest). Two consumers read the CHEST-slot flag: `LivingEntityMixin`'s `famspecial$undeadResistance` ModifyReturnValue multiplies damage by 0.4× when the attacker's type is in `EntityTypeTags.UNDEAD` (arrow hits attribute back to the shooter via `DamageSource.getAttacker()`, so skeletons + strays are covered by the same branch); `ArmorEffects`' `undead_resistance_chest` bonus applies the cosmetic `UNDEAD_RESISTANCE` HUD badge (display name "Undead Resistance").
- `famspecial:applies_wither_on_hit` (`Boolean`) — main-hand weapon flag that applies **Wither IV** on any melee hit — **8s vs mobs / 2s vs players** (PvE/PvP duration split mirroring Messmer's Venom). Lives on Thriller's Edge (Necromancer axe). Handled additively in `AttackHandlers`' `ALLOW_DAMAGE` handler alongside the Messmer's Venom branch (main-hand only; off-hand flagged stacks don't propagate). On apply, mob victims (never players) are added to a `Set<UUID>` tracked in `AttackHandlers`; an `AFTER_DEATH` hook drops a `WITHER_ROSE` at the death position when a tagged mob dies to `DamageTypes.WITHER`. A 200-tick (10 s) `END_SERVER_TICK` sweep prunes UUIDs whose entities are no longer loaded. `ArmorEffects`' `wither_touch_mainhand` bonus applies the cosmetic `WITHER_TOUCH` HUD badge.
- `famspecial:necromancer_cooldown_end` (`Long`) — absolute world-tick at which the 5-min Necromancer summon cooldown expires. Stamped on every worn Necromancer piece by `NecromancerSummon.stampCooldownOnWornSet` when the summon fires. Client tooltips read this to render the MM:SS countdown next to the "Grants Rotten Muscle" line; server ignores it entirely and keeps using its own per-UUID `lastSummonTick` map (stamping is a display-sync mechanism, not the cooldown source of truth). Stays stamped after expiry — the tooltip just hides once `currentTime >= value`.
- `famspecial:grants_ominous` (`Boolean`) — boots flag driving Bad Omen while worn at night (timeOfDay ≥ 14000, i.e. 8 PM → 6 AM) or always in Nether/End. Lives on the Esh-Endra-Navesh boots (Dhoom Machale). Applied by `ArmorEffects`' `ominous_boots` bonus. Bad Omen is a vanilla effect, so it's deliberately left out of `MOD_MANAGED` — ambient sources (ominous-bottle, raid captain drops) decay naturally rather than being stripped on unequip.
- `famspecial:bonus_diamond_chance` (`Boolean`) — main-hand pickaxe flag. Every successful block break additionally rolls vanilla's `blocks/diamond_ore` loot table at 1/700, respecting Fortune + Silk Touch because the flagged pickaxe is passed back in as the `TOOL` of the phantom drop. Lives on Just Hit Bro (Esh-Endra-Navesh pickaxe). Handled in `BlockBreakHandler` on `PlayerBlockBreakEvents.AFTER`; advertised via the cosmetic `SHADI_BUFF` HUD badge (`ArmorEffects`' `shadi_buff_mainhand` bonus) while the pickaxe is held.
- `famspecial:ignores_kb_resistance` (`Boolean`) — main-hand weapon flag driving **Emperor's Divide**. Lives on the Sun Disc Spear (Shurima). While the stack is in the mainhand, `ArmorEffects`' `emperors_divide_mainhand` bonus applies `EMPERORS_DIVIDE`; `LivingEntityKnockbackMixin` reads that effect on the attacker in `LivingEntity#takeKnockback` and returns `0.0` from the victim's `KNOCKBACK_RESISTANCE` attribute lookup, so the `(1 - resist)` multiplier becomes 1 and knockback always lands full-strength regardless of target stiffness. Instant removal on scroll/hotbar-key swap via `ServerPlayNetworkHandlerMixin`; MOD_MANAGED fallback catches drag / drop / item break.
- `famspecial:grants_suns_protection` (`Boolean`) — boots flag driving **Sun's Protection**. Lives on Drifting Sands (Shurima boots). Applied by `ArmorEffects`' `suns_protection_boots` bonus **whenever the boots are worn** — the prior **desert-family biome gate was removed** and the bonus id renamed `suns_protection_boots_desert` → `suns_protection_boots` at the same time; `LivingEntityMixin`'s `modifyAppliedDamage` reads the effect on the victim and scales incoming damage by 0.8× (Resistance-I-equivalent) globally. **Not in `MOD_MANAGED`** — on boots removal the trigger simply stops re-upping and the effect decays naturally over ~20 s. The `refreshBonusesFor` call from `LivingEntityEquipMixin` does cause the boots-remove case to stop re-upping within one tick, but there is no explicit strip — the lingering damage reduction rides out the remaining duration. **Note**: Shuriman Endurance (4/4) still gates on desert-family biome — only Sun's Protection went global.
- `famspecial:lightning_on_hit` (`Boolean`) — main-hand weapon flag. Lives on Mjolnir. Every successful melee hit rolls 100% (thundering) / 20% (clear) to spawn a `LightningEntity` at the victim. Handled in `ThorEffects.AFTER_DAMAGE`, gated on the attacker's mainhand. `ArmorEffects`' `god_of_thunder_mainhand` bonus applies the cosmetic `GOD_OF_THUNDER` HUD badge so icon + mechanic share one source of truth.
- `famspecial:triggers_storm_awakening` (`Boolean`) — leggings flag. Lives on Warrior's Greaves (Thor leggings). On every kill the wearer performs, roll the Storm's Awakening trigger chance (**flat 8%**, `STORM_TRIGGER_CHANCE = 0.08f`) to start a thunderstorm — but **only when `world.isThundering()` is false**; if already thundering, the kill is ignored entirely (no roll, no cooldown burn). Storm duration is **2400 t (~2 min real-time)**. Per-player **24000-tick (1 MC day)** cooldown only arms on successful trigger. Handled in `ThorEffects.AFTER_DEATH`. On successful trigger, `STORM_COOLDOWN_END` is stamped onto the worn Warrior's Greaves for the client tooltip's crimson MM:SS countdown (parallel to the Necromancer cooldown component). `ArmorEffects`' `storms_awakening_legs_ready` bonus applies the cosmetic `STORMS_AWAKENING` HUD badge while the leggings are worn AND the cooldown is clear — disappears during the 1-MC-day window, re-appears once cleared.
- `famspecial:storm_cooldown_end` (`Long`) — absolute world-tick at which the Storm's Awakening cooldown expires. Stamped on the worn Warrior's Greaves by `ThorEffects` on a successful storm trigger — parallel to `NECROMANCER_COOLDOWN_END`. Client tooltips read this via `FamSpecialClient.stormCooldownRemaining(ItemStack)` to render the crimson MM:SS countdown next to the "Grants Storm's Awakening" line through `grantLineWithCooldown(...)`. Server ignores this component entirely — the cooldown source of truth is `ThorEffects`' per-UUID map; stamping is a display-sync mechanism.
- `famspecial:technoblade_totem_cooldown_end` (`Long`) — absolute world-tick at which the **5-minute (6000 t) Technoblade totem-save cooldown** expires (shortened from the earlier 30-minute / 36000-tick value). Stamped on **every worn Raider piece** by `TechnobladeSave.tryConsume` on a successful save. Parallel in shape to `STORM_COOLDOWN_END` / `NECROMANCER_COOLDOWN_END`, but unlike those two **the stamp itself is the cooldown source of truth** — there is no parallel per-UUID map. `TechnobladeSave.isOnCooldown` reads the max-stamped tick across all 4 armor slots; client tooltips read this via `FamSpecialClient.technobladeCooldownRemaining(ItemStack)` to render the crimson MM:SS countdown next to "Grants Technoblade Never Dies" through `grantLineWithCooldown(...)`. **Appended to `MOD_COMPONENTS_BASE`** so it carries through the chestplate→elytra merge (so a merged elytra preserves an in-flight cooldown stamp); stays stamped after expiry (tooltip just hides once `currentTime >= value`).
- `famspecial:thor_mace` (`Boolean`) — marker identifying Mjolnir specifically (not any `MaceItem` instance). Drives the 5th-piece half of the Thor 5/5 gate in `ArmorEffects`' `asgardians_flight_full_thor_set_and_mjolnir` bonus, the right-click riptide launch's weapon check in `MjolnirMaceItem.use` / `onStoppedUsing`, and the mainhand strip check in `ServerPlayNetworkHandlerMixin`. Distinct from `LIGHTNING_ON_HIT` so one flag can in theory be reused on non-Mjolnir Thor weapons without making them count as the 5th piece. **Does not gate anvil behavior** — Mjolnir is anvil-modifiable per vanilla mace rules; the earlier `AnvilScreenHandlerMixin` TAIL guard that blanked Mjolnir+book anvil outputs has been removed.
- `famspecial:bounty_hunter` (`Boolean`) — leggings flag. Lives on Trousers of the Trail (Raider's Legacy legs). Drives the **Bounty Hunter** mechanic in `BountyHunterKills` — when the wearer kills a mob with a vanilla bow/crossbow arrow (incl. spectral arrows) **or** a crossbow-fired firework rocket, **10% chance** (`DROP_CHANCE = 0.10f`, bumped from `0.05f`) to drop 1–3 diamonds at the corpse and play `ModSounds.INDY`. Also drives `ArmorEffects`' `bounty_hunter_legs` bonus, which applies the cosmetic `BOUNTY_HUNTER` HUD badge while the leggings are worn so badge + mechanic share one source of truth. Appended to `MOD_COMPONENTS_BASE` so it carries through any future component-copy path.
- `famspecial:crusaders_volley` (`Boolean`) — main-hand crossbow flag. Lives on Fortune & Glory (Raider's Legacy crossbow). Read by `FortuneGloryItem.onStoppedUsing` to schedule the 2-tick- and 4-tick-delayed follow-up shots, and by `ArmorEffects`' `crusaders_volley_mainhand` bonus to apply the cosmetic `CRUSADERS_VOLLEY` HUD badge while the crossbow is held. Follow-up shots inherit Multishot/Piercing levels from the source stack at fire time. Appended to `MOD_COMPONENTS_BASE`.

**Component-copy helpers (`copyModComponents` / `copyModComponentsForElytra`).** The file exposes two static copy methods that transplant the mod's custom components from a source stack to a destination stack. Both iterate over the shared constant `MOD_COMPONENTS_BASE` — the canonical allowlist of every themed `ComponentType` that should carry across a stack merge. `copyModComponents(from, to)` carries the full list (reserved for armor-to-armor transplant paths; not used by the current chestplate→elytra merge). `copyModComponentsForElytra(from, to)` is the variant invoked by `AnvilScreenHandlerMixin.famspecial$buildMergedElytra` and `GrindstoneScreenHandlerMixin.famspecial$restampMergedElytra` — it skips any entry listed in `ELYTRA_EXCLUDED_COMPONENTS` (currently just `INDESTRUCTIBLE`). Rationale: `INDESTRUCTIBLE` is redundant on elytra because vanilla already caps elytra damage at `maxDamage - 1` natively (the `ItemStackMixin` comment block that documents our cap is explicitly modeled after that vanilla elytra behavior), and carrying it over would interact badly with the broken-state enchant-hiding mixins. `REGENS_DURABILITY` **is** intentionally transferred — the merged elytra keeps the source theme's regen-managed durability design. `BLOCKS_MENDING` is also transferred, but is currently a no-op flag (no consumer reads it) — kept in the copy path for forward compatibility.

**Discipline for new components.** Any new `ComponentType` added to this file MUST be appended to `MOD_COMPONENTS_BASE` or it won't carry across any merge path. If the new component shouldn't follow the chestplate→elytra merge (e.g. it's semantically slot-specific or would enable an exploit on the elytra slot), also add it to `ELYTRA_EXCLUDED_COMPONENTS`.

## `effect/ModStatusEffects.java`

Registers custom `StatusEffect` entries as `RegistryEntry<StatusEffect>` values. Two effects carry real gameplay; the rest are cosmetic HUD badges (registered purely so `ArmorEffects.BONUSES` can apply them as top-right icons that truthfully track when their underlying mechanic is firing).

- `MESSMERS_FLAME_AEGIS` (BENEFICIAL) — marker effect checked by `LivingEntityMixin` to scale incoming fire damage to 20%. Driven by the `GRANTS_MESSMERS_FLAME` helmet flag.
- `MESSMERS_VENOM` (BENEFICIAL) — dual-purpose. On the **wearer**, it's a cosmetic aura (the `applyUpdateEffect` override short-circuits damage for players wearing the full Fire Serpent set via the `hasFullSet(player, "fire_serpent")` check at the top of `applyUpdateEffect`) plus the trigger `AttackHandlers` reads to decide whether a player's hit propagates venom — **weapon-agnostic**, any successful damage event the wearer deals to anyone other than themselves carries it (fists, sword, axe, bow/arrow, snowball, anything). On **other targets**, the override ticks every 25 game-ticks at amp 0 and deals magic damage — **5.0 on mobs, 1.0 on other players** (PvE/PvP split). **Lethal** — no HP floor, targets can die from venom alone. Magic damage bypasses armor and protection enchantments. `AttackHandlers` applies with a **124-tick (6.2 s) duration** whose `duration % 25 == 0` modulo alignment means the 4 hits land at duration 100 / 75 / 50 / 25 (T+25 / T+50 / T+75 / T+100 ticks after application) — all past the application's 10-tick i-frame window, so each hit lands at full strength for a deterministic **20 HP per cycle on mobs, 4 HP on other players**. Every fresh hit refreshes the duration. A distinct registry entry from `StatusEffects.POISON` so vanilla `canHaveStatusEffect` equality checks (spiders, undead, the Wither, the Ender Dragon) fall through and the effect applies to every mob.
- `HEALERS_VISION`, `EMPOWERED_RIPTIDE`, `NIGHT_STRENGTH`, `TIRELESS_LUNGE` — cosmetic HUD badges only; no override logic. Colors are tuned to each set's name-prefix palette (cyan / sky-blue / indigo / neon-green). Each is driven by a `Bonus.effects(...)` entry in `ArmorEffects.BONUSES` whose trigger matches the gate of the mechanic it's advertising, so icon and gameplay share one trigger and can't drift. `NIGHT_STRENGTH` is **not in `MOD_MANAGED`** — decays naturally over ~20 s after the gate drops. `NIGHT_STRENGTH`'s icon at `mob_effect/night_strength.png` is now copied from vanilla `wither_rose.png` (previously from vanilla `strength.png`). The remaining three in this row are in `MOD_MANAGED`.
- `STORMLIGHT` (BENEFICIAL, color `0xA0D2FF`) — HUD badge + particle aura for the Knight Radiant Shard set. `applyUpdateEffect` spawns End Rod particles around the wearer every 5 ticks (throttled via `canApplyUpdateEffect`); `canApplyUpdateEffect` is the only non-cosmetic override. The durability-regen mechanic it advertises lives in `ArmorEffects`' 243-tick block, driven by `REGENS_DURABILITY` — the effect itself does not regen anything. Icon copied from vanilla `conduit_power.png`.
- `SHARDBEARING` (BENEFICIAL, color `0xE0F0FF`) — cosmetic HUD badge for Oathbringer. Actual gameplay (+10% of the target's max HP as bonus damage on the wearer's melee hits, applied past armor/protection/resistance) lives in `LivingEntityMixin`'s `modifyAppliedDamage` ModifyReturnValue at `LivingEntityMixin.java:61` (factor `OATHBRINGER_HP_DAMAGE_FRACTION = 0.10f`), gated on the attacker's mainhand carrying `GRANTS_SHARDBEARING`. Hooked on `modifyAppliedDamage` rather than `applyArmorToDamage` so the bonus lands AFTER all mitigation — matters on unarmored mobs (most of them) where a pre-armor bonus would have been absorbed or eaten. Icon and mixin share the same mainhand gate via `ArmorEffects`' `shardbearing_mainhand` bonus. No time gate. Icon copied from vanilla `item/prismarine_shard.png` (literally a shard — on-theme for the effect name and the Knight Radiant pale-blue palette).
- `RADIANT_MIGHT` (BENEFICIAL, color `0xE0F0FF`) — Knight Radiant 4/4 set bonus. Registers `+8.0 ATTACK_DAMAGE` (between vanilla Strength II's +6.0 and Strength III's +9.0) as an `ADD_VALUE` modifier via `.addAttributeModifier(...)` chained on registration, so vanilla `StatusEffect` application handles the damage boost automatically — no `applyUpdateEffect` override. Driven by `ArmorEffects`' `knight_radiant_full_set` bonus (all four armor slots share `SET_ID="knight_radiant"`); no time gate. Distinct from the per-piece Stormlight aura. **Not in `MOD_MANAGED`** — decays naturally over ~20 s after the set is broken rather than being diff-stripped within a 4 s cycle. Icon at `mob_effect/radiant_might.png`, copied from vanilla `strength.png`.
- `ROTTEN_MUSCLE` (BENEFICIAL, color `0x8B0000`, display name "Rotten Muscle") — Necromancer 4/4 HUD badge advertising the Zombie-Reinforcements charge state. Applied by `ArmorEffects`' `rotten_muscle_ready` bonus only while the full set is worn AND `NecromancerSummon.isOnCooldown(...)` returns `false` — so the badge appears when the summon is charged and disappears during the 5-minute cooldown window, then re-appears when the cooldown elapses. On successful summon, `NecromancerSummon` calls `player.removeStatusEffect(ROTTEN_MUSCLE)` to flip the HUD instantly instead of waiting for the 20s duration to tick out. `canApplyUpdateEffect` + `applyUpdateEffect` spawn a single `ParticleTypes.SOUL` wisp around the wearer every 10 ticks — the "necromantic aura" visual cue that the summon is primed. Applied in `ArmorEffects` with `showParticles=false` so the vanilla ambient bubbles don't overlap the themed soul wisps. Icon copied byte-for-byte from vanilla `mob_effect/wither.png`.
- `UNDEAD_RESISTANCE` (BENEFICIAL, color `0x6B8E5A`, display name "Undead Resistance") — Necromancer chestplate (Smooth Criminal's Vestment) HUD badge + particle aura. Advertises the 60% undead-damage reduction. The reduction itself lives in `LivingEntityMixin`, gated on the CHEST-slot `GRANTS_UNDEAD_RESISTANCE` component — effect and mixin share one flag so they can't drift. `canApplyUpdateEffect` + `applyUpdateEffect` spawn `LARGE_SMOKE` every 12 ticks (the one Necromancer effect that still radiates visible particles — the green smoke is the visual cue for the piece-alone bonus). Icon copied byte-for-byte from vanilla `mob_effect/resistance.png`.
- `WITHER_TOUCH` (BENEFICIAL, color `0xA8C49A`) — Thriller's Edge (Necromancer axe) HUD badge + particle aura. Advertises the Wither-IV-on-hit gameplay (8s vs mobs / 2s vs players, mob wither-kills drop a wither rose), which itself lives in `AttackHandlers`' `ALLOW_DAMAGE` branch gated on the mainhand `APPLIES_WITHER_ON_HIT` flag. `canApplyUpdateEffect` + `applyUpdateEffect` spawn 2 `ParticleTypes.ASH` flakes every 6 ticks around the wearer's upper body — the ash-puff cue that a next hit will wither. Applied in `ArmorEffects` with `showParticles=false` so the vanilla ambient bubbles don't overlap the themed ash. Icon copied byte-for-byte from vanilla `mob_effect/wither.png` (shared sprite with `ROTTEN_MUSCLE` — same wither-skull motif, differentiated by the color tint on each effect's registration).
- `SHADI_BUFF` (BENEFICIAL, color `0xE8D171`) — Just Hit Bro (Esh-Endra-Navesh pickaxe) HUD badge. Purely cosmetic — the actual gameplay (1/700 bonus-diamond roll per block break) lives in `BlockBreakHandler` gated on the mainhand `BONUS_DIAMOND_CHANCE` flag. Applied by `ArmorEffects`' `shadi_buff_mainhand` bonus while the pickaxe is held, so badge + gameplay share one source of truth. No time gate — pops on swap to pickaxe, drops on swap away. Color matches the Esh-Endra-Navesh `NAME_PIECE` palette (gold). Icon at `mob_effect/shadi_buff.png` is vanilla `item/diamond.png` (16×16) padded with a 1px transparent border to match the 18×18 mob_effect convention.
- `SAGES_GRACE` (BENEFICIAL, color `0x9FE59F`, no particle override) — Sage's Grace sword (Pacifist theme) HUD badge. Registered as a plain `new StatusEffect(BENEFICIAL, 0x9FE59F) {}` — no overrides. Purely cosmetic — the actual heal-on-hit gameplay lives in `AttackHandlers`' `AttackEntityCallback` branch gated on the mainhand `HEALS_TARGET` flag. Applied by `ArmorEffects`' `sages_grace_mainhand` bonus while the sword is held, so badge + gameplay share one source of truth. No time gate. In `MOD_MANAGED`. Particles are suppressed in the apply loop (added to the `showParticles=false` list alongside `ROTTEN_MUSCLE` and `WITHER_TOUCH`). Icon at `assets/famspecial/textures/mob_effect/sages_grace.png` is vanilla `item/golden_apple.png` (16×16) padded with a 1px transparent border to 18×18 per the mob_effect convention. Lang key `effect.famspecial.sages_grace` → "Sage's Grace". The tooltip keyword "Grants Sage's Grace" on the sword (renamed from the older "Pacifist Strike" label) is rendered via `grantLine(...)` in `FamSpecialClient`. Note: the sword *item name* is still "Sage's Grace" and `PacifistItems.java` still uses the `HEALS_TARGET` flag — only the status-effect badge (and the tooltip keyword it advertises) is renamed.
- `EMPERORS_DIVIDE` (BENEFICIAL, color `0xC8A84B` — Shurima `NAME_PREFIX` worn-gold) — Sun Disc Spear (Shurima) gameplay-predicate marker + HUD badge. Registered as a plain `new StatusEffect(BENEFICIAL, 0xC8A84B) {}` — no overrides. `LivingEntityKnockbackMixin` reads `attacker.hasStatusEffect(EMPERORS_DIVIDE)` in `LivingEntity#takeKnockback` and returns `0.0` from the victim's `KNOCKBACK_RESISTANCE` lookup so the `(1 - resist)` multiplier becomes 1 — knockback always lands full-strength. Applied by `ArmorEffects`' `emperors_divide_mainhand` bonus on the 80-tick BONUSES cadence while the mainhand carries `IGNORES_KB_RESISTANCE`. In `MOD_MANAGED`, plus event-stripped by `ServerPlayNetworkHandlerMixin` on hotbar swap. Icon copied byte-for-byte from vanilla `mob_effect/strength.png`.
- `SUNS_PROTECTION` (BENEFICIAL, color `0xE8922A` — Shurima `LORE_ACCENT` warm orange) — Drifting Sands (Shurima boots) gameplay-predicate marker + HUD badge. Registered as a plain `new StatusEffect(BENEFICIAL, 0xE8922A) {}` — no overrides. `LivingEntityMixin`'s `modifyAppliedDamage` ModifyReturnValue reads `victim.hasStatusEffect(SUNS_PROTECTION)` and scales incoming damage by 0.8× (Resistance-I-equivalent). Applied by `ArmorEffects`' `suns_protection_boots` bonus **whenever the boots are worn** — the prior desert-family biome gate was removed and the bonus id was renamed `suns_protection_boots_desert` → `suns_protection_boots` at the same time. **Not in `MOD_MANAGED`** — on boots removal the trigger stops re-upping and the effect decays naturally over ~20 s. Icon copied byte-for-byte from vanilla `mob_effect/resistance.png`.
- `SHURIMAN_ENDURANCE` (BENEFICIAL, color `0xB8A070` — Shurima `LORE_BASE` dusty parchment) — Shurima 4/4 gameplay marker + HUD badge. Registered as a plain `new StatusEffect(BENEFICIAL, 0xB8A070) {}` — no overrides. `PlayerEntityExhaustionMixin` HEAD-cancels `PlayerEntity#addExhaustion` whenever the player has this effect, so no saturation loss accrues from any source (sprint, jump, attack, regen-from-food, block break, mob damage). The **only beacon-style (non-`MOD_MANAGED`) Shurima effect** — applied by `ArmorEffects`' `shuriman_endurance_full_set_desert` bonus via a dedicated branch in the dispatch loop with a **4800-tick (4 min) duration** and an **apply-once gate**: skip re-application while remaining > `SHURIMAN_ENDURANCE_REFRESH_THRESHOLD_TICKS` (4720 t). Trigger: 4/4 Shurima worn AND the player is in a desert-family biome (`DESERT`, `BADLANDS`, or `ERODED_BADLANDS`). The apply-once gate is what makes it persist anywhere the player walks for the full ~4 minutes once earned in the desert — without it, leaving the biome mid-buff would clobber the timer back to the 400-tick default. Decays naturally when the timer expires; cancellation lives in `PlayerEntityExhaustionMixin`.
- `GOD_OF_THUNDER` (BENEFICIAL, color `0x60C8FF` — Thor `LORE_ACCENT` lightning cyan) — Mjolnir mainhand HUD badge. Cosmetic — actual gameplay (per-hit lightning roll: 100% thundering / 20% clear) lives in `ThorEffects.AFTER_DAMAGE`, gated on the attacker's mainhand `LIGHTNING_ON_HIT` flag. Applied by `ArmorEffects`' `god_of_thunder_mainhand` bonus on the same flag so badge + mechanic share one source of truth. In `MOD_MANAGED`, plus event-stripped by `ServerPlayNetworkHandlerMixin` on hotbar swap. Icon copied byte-for-byte from vanilla `mob_effect/strength.png`.
- `STORMS_AWAKENING` (BENEFICIAL, color `0x6858B8` — Thor `LORE_BASE` deep storm purple) — Warrior's Greaves "ready-state" HUD badge. Cosmetic ability-armed indicator — actual gameplay (8% roll on kill → start thunderstorm of 2400 t / ~2 min duration; skip if already thundering) lives in `ThorEffects.AFTER_DEATH`, gated on the leggings' `TRIGGERS_STORM_AWAKENING` flag and the per-player **24000-tick (1 MC day)** cooldown. Applied by `ArmorEffects`' `storms_awakening_legs_ready` bonus only while the leggings are worn AND `ThorEffects.isStormCooldown(...)` returns false — so the badge is visible when the ability is armed and disappears during the 1-MC-day window after a successful trigger, then re-appears once cleared. In `MOD_MANAGED`. Icon copied byte-for-byte from vanilla `mob_effect/bad_omen.png`.
- `ASGARDIANS_FLIGHT` (BENEFICIAL, color `0x60C8FF`) — Thor 5/5 gameplay-predicate marker. Registered inline as a plain `new StatusEffect(BENEFICIAL, 0x60C8FF) {}` — no overrides. Drives Mjolnir's right-click riptide launch via `player.hasStatusEffect(ASGARDIANS_FLIGHT)` checks in `MjolnirMaceItem.use` and `MjolnirMaceItem.onStoppedUsing`; the launch short-circuits to `super` whenever the effect is absent. Applied by `ArmorEffects`' `asgardians_flight_full_thor_set_and_mjolnir` bonus only while the four Thor armor pieces are worn AND `THOR_MACE` is in the main hand. In `MOD_MANAGED`, plus event-stripped by `LivingEntityEquipMixin` (armor-half) and `ServerPlayNetworkHandlerMixin` (mainhand-half) so either half of the 5/5 gate breaking removes the effect on the same tick. Icon at `mob_effect/asgardians_flight.png` is vanilla `item/elytra.png`.
- `BOUNTY_HUNTER` (BENEFICIAL, color `0xA47449` — whip brown) — Raider's Legacy leggings (Trousers of the Trail) HUD badge. Registered as a plain `new StatusEffect(BENEFICIAL, 0xA47449) {}` — no overrides, no particles. Cosmetic — actual gameplay (**10% chance** to drop 1–3 diamonds + `RAJ_TOTEM` sound on arrow or firework-rocket kills) lives in `BountyHunterKills`, gated on the leggings' `BOUNTY_HUNTER` flag. Applied by `ArmorEffects`' `bounty_hunter_legs` bonus while the leggings are worn so badge + mechanic share one source of truth. In `MOD_MANAGED`. Icon at `mob_effect/bounty_hunter.png` derived from vanilla `item/lead.png`.
- `TECHNOBLADE_FEATHERWEIGHT` (BENEFICIAL, display name "Featherweight") — Raider's Legacy 4/4 set bonus + gameplay-predicate marker. Registered as a plain `new StatusEffect(BENEFICIAL, ...) {}` — no overrides, no particles. Drives fall-damage immunity via the 5th `@ModifyReturnValue` in `LivingEntityMixin` (`famspecial$technobladeFallImmunity`) — when the victim is a player with this effect AND `source.isOf(DamageTypes.FALL)`, returns 0.0f. Applied by `ArmorEffects`' `technoblade_never_dies_full_set` bonus alongside `TECHNOBLADE_NEVER_DIES` (the set wearer gets both icons in the HUD) only while all four armor slots share `SET_ID="raider"`. **No cooldown — always-on while 4/4 worn.** In `MOD_MANAGED`. Icon at `mob_effect/technoblade_featherweight.png` is a byte-for-byte copy of vanilla Slow Falling (feather).
- `TECHNOBLADE_NEVER_DIES` (BENEFICIAL, color `0xF4C430` — idol gold) — Raider's Legacy 4/4 set bonus + gameplay-predicate marker. Registered as a plain `new StatusEffect(BENEFICIAL, 0xF4C430) {}` — no overrides, no particles. Drives the **totem-of-undying auto-save** via the 6th `@ModifyReturnValue` in `LivingEntityMixin` (`famspecial$technobladeTotemSave`) — see `TechnobladeSave` section below; the 30 minute cooldown was shortened to **5 minutes (6000 t)**. Applied by `ArmorEffects`' `technoblade_never_dies_full_set` bonus alongside `TECHNOBLADE_FEATHERWEIGHT` only while all four armor slots share `SET_ID="raider"`. In `MOD_MANAGED`. Icon at `mob_effect/technoblade_never_dies.png` derived from vanilla `entity/piglin/piglin_head.png` (or equivalent `piglin_head` sprite).
- `CRUSADERS_VOLLEY` (BENEFICIAL, color `0xC19A6B` — fedora tan) — Fortune & Glory mainhand HUD badge. Registered as a plain `new StatusEffect(BENEFICIAL, 0xC19A6B) {}` — no overrides, no particles. Cosmetic — actual gameplay (single right-click charge fires three shots back-to-back at a 2-tick cadence; shots 2 and 3 inherit Multishot/Piercing) lives in `FortuneGloryItem`, gated on the crossbow's `CRUSADERS_VOLLEY` flag. Applied by `ArmorEffects`' `crusaders_volley_mainhand` bonus while the crossbow is held. In `MOD_MANAGED`. Icon at `mob_effect/crusaders_volley.png` derived from vanilla `item/crossbow_standby.png`.

All icon PNGs at `assets/famspecial/textures/mob_effect/*.png` are **byte-for-byte copies of vanilla icons** extracted from the Fabric Loom cache jar — most from `assets/minecraft/textures/mob_effect/` (`fire_resistance.png`, `poison.png`, `glowing.png`, `conduit_power.png`, `strength.png`, `saturation.png`, `wither.png`, `resistance.png`, `bad_omen.png`), plus `assets/minecraft/textures/item/prismarine_shard.png` for Shardbearing (literal shard, pale-blue matches the Knight Radiant palette), `assets/minecraft/textures/item/diamond.png` for Shadi Buff (literal diamond, padded to 18×18 with a 1px transparent border to match the mob_effect convention), `assets/minecraft/textures/item/golden_apple.png` for Sage's Grace (same 18×18-padding convention), `assets/minecraft/textures/item/wither_rose.png` for Nightfall's Might (same 18×18-padding convention; `night_strength.png` was previously copied from vanilla `strength.png` and has now been re-sourced from wither_rose), and `assets/minecraft/textures/item/elytra.png` for Asgardian's Flight. Keep this convention — custom icons are not authored from scratch.

## `item/ModItemGroups.java`

Registration hub + shared helpers, not the item catalog. Registers **two** sibling creative tabs:

- **`GEAR_GROUP`** (id `famspecial:gear`, icon = `IRON_PICKAXE`, lang key `itemgroup.famspecial.gear`). Its `.entries(...)` lambda calls every theme's `addWeapons(entries, enchants)` first (all weapons, grouped by type across themes), then every theme's `addTools(entries, enchants)`. Theme order in both blocks is `OldFam → Mistborn → Pacifist → Poseidon → FireSerpent → Necromancer → KnightRadiant → EshEndraNavesh → Shurima → Thor → Raider`.
- **`ARMOR_GROUP`** (id `famspecial:armor`, icon = `IRON_CHESTPLATE`, lang key `itemgroup.famspecial.armor`). Its `.entries(...)` lambda calls every theme's `addArmor(entries, enchants)` in the same theme order — giving a contiguous block of helmet → chest → legs → boots per set.

Each theme file exposes three methods (`addWeapons` / `addTools` / `addArmor`) with empty bodies for categories that theme doesn't ship — e.g. EshEndraNavesh has no weapons, Mistborn has no tools. Within a theme the order is vanilla-page-aligned: weapons = sword(s) → axe → bow → trident; tools = pickaxe → shovel → hoe; armor = helmet → chest → legs → boots.

Exposes public helpers consumed by the `entries/` subpackage:

- `buildItem(item, name, lore, enchants, EnchantEntry...)` — applies `CUSTOM_NAME`, `LORE`, and `ENCHANTMENTS` components to a fresh stack. Also stamps `IS_FAMSPECIAL_GEAR=true` on every stack it produces (immediately after the `CUSTOM_NAME` / `LORE` writes, at `ModItemGroups.java:132`). This is the single chokepoint covering all themed items across all 11 theme groups (including OldFam) — weapons, tools, and armor all route through this helper, so the identity flag propagates to creative-tab stacks and to loot-injected stacks built via `LootPoolHelpers.namedItem(...)` which mirrors the same stamp. The stamp lives at the per-stack layer, **not** the `Item.Settings` default-component layer, because `ModItemGroups` reuses vanilla item instances (`Items.DIAMOND_CHESTPLATE`, etc.) rather than registering custom `Item` subclasses with a `Settings` builder — the per-stack layer is the only universal layer for this codebase's item-registration shape.
- `loreLine(base, segments...)` — mixes inline `String`/`Text` segments on a shared base style so a single lore line can have accent-styled words.
- `e(key, level)` / `EnchantEntry` — fixed-level enchants for creative-tab display (loot-table enchants use ranges — see `LootPoolHelpers`).

Items with passive effects set their component flags on the stack here before adding, e.g. `stack.set(ModComponents.GRANTS_NIGHT_VISION, true)`.

## `sound/ModSounds.java`

Registers custom `SoundEvent`s via `Registry.register(Registries.SOUND_EVENT, ...)`. Currently exposes three entries:

- `NECROMANCER_SUMMON` (id `famspecial:necromancer_summon`) — played by `NecromancerSummon` on a successful zombie spawn. Backed by `assets/famspecial/sounds/zombiezzz.ogg`.
- `BONUS_DIAMOND` (id `famspecial:bonus_diamond`) — played by `BlockBreakHandler` when a `BONUS_DIAMOND_CHANCE` roll succeeds. Backed by `assets/famspecial/sounds/lucky.ogg`.
- `INDY` (id `famspecial:indy`, renamed from the earlier `RAJ_TOTEM` / `famspecial:raj_totem`) — played by `BountyHunterKills` when a 10% Bounty Hunter arrow-or-firework-rocket-kill roll succeeds. Backed by `assets/famspecial/sounds/indy.ogg`.

Event ids are wired to their OGG assets via `assets/famspecial/sounds.json` (`"<event>": { "sounds": [ { "name": "famspecial:<basename>", "stream": false } ] }`). To add a new sound: register the event here, drop the OGG under `assets/famspecial/sounds/`, add a `sounds.json` entry mapping the event id to the asset path.

## `entity/ModEntities.java` and `entity/VolleyArrowEntity.java`

Custom `EntityType` registry plus the single custom entity that uses it. Wired from `FamSpecial.onInitialize` via `ModEntities.register()` (early init, alongside `ModSounds.register()` / `ModComponents.register()`) and from `FamSpecialClient.onInitializeClient` via `EntityRendererRegistry.register(ModEntities.VOLLEY_ARROW, ArrowEntityRenderer::new)` (the renderer is reused verbatim from vanilla — no custom texture or model).

`ModEntities.VOLLEY_ARROW` is built via `EntityType.Builder.create(VolleyArrowEntity::new, SpawnGroup.MISC).dropsNothing().dimensions(0.5F, 0.5F).eyeHeight(0.13F).maxTrackingRange(4).trackingTickInterval(20).build(VOLLEY_ARROW_KEY)` — these settings mirror vanilla `EntityType.ARROW` exactly so volley arrows track and serialize to the client identically to a regular arrow.

`VolleyArrowEntity` extends `ArrowEntity` and exists solely to carry the source crossbow's weapon-stack + Piercing level across the 2-tick volley cadence so shots 2 and 3 resolve damage scaling and pierce count identically to a vanilla crossbow-launched arrow even though the original `CHARGED_PROJECTILES` component is gone by the time they fire. **It does NOT touch iframes** — the iframe bypass for Crusader's Volley is now a unified `ALLOW_DAMAGE` hook in `FortuneGloryItem` that keys off a `Set<UUID>` of F&G-tagged projectiles (covering both arrows and fireworks); see the `FortuneGloryItem` section below.

Two constructors. The deserialization-side `VolleyArrowEntity(EntityType<? extends VolleyArrowEntity>, World)` is required by the entity-spawn pipeline (client receives a spawn packet for the custom EntityType, looks up the factory, calls this ctor). The convenience-side `VolleyArrowEntity(World, LivingEntity owner, ItemStack arrowStack, ItemStack crossbowStack)` is what `FortuneGloryItem.fireScheduledShot` uses — it can't simply chain through `ArrowEntity`'s convenience constructor because that hardcodes `EntityType.ARROW` and would make the spawn packet say ARROW (defeating the custom-entity-on-client purpose). Instead it chains through the deserialization super, then manually replicates `PersistentProjectileEntity`'s convenience-ctor body: `setStack(arrowStack.copy())`, `copyComponentsFrom(arrowStack)`, INTANGIBLE_PROJECTILE pickup-permission handling, `setPosition(...)`, then stashes the crossbow stack into a private `weaponStack` field and computes Piercing into a private `cachedPierceLevel` byte (the parent's `weapon` field and `setPierceLevel` are private with no setters, so `getWeaponStack()` and `getPierceLevel()` are overridden to return the stashed values). Damage-scaling enchant lookup (Power, Smite-on-arrow, etc.) reads through the overridden `getWeaponStack()`; piercing reads through the overridden `getPierceLevel()`. Net effect: vanilla hit behavior end-to-end — no `onEntityHit` override.

## `item/NecromancerSummon.java`

Zombie Reinforcements, the 4/4 Necromancer set bonus. Event-driven (not tick-driven), registers four hooks from `onInitialize`:

1. **`ServerLivingEntityEvents.AFTER_DAMAGE`** — the summon trigger. Fires after mitigation, only when the victim survived, carrying the post-mitigation `DamageSource`. On every hit taken by a 4/4-set-wearer with a living attacker, append the attacker's UUID to the per-summoner `attackersBySummoner` set (summoner UUID → `LinkedHashSet<UUID>`, FIFO eviction at `MAX_ATTACKERS_PER_SUMMONER = 16`, via the `addAttacker` helper) and call `nudgeIdleZombies` (renamed from the older `retargetActiveZombies`) — which only sets a new target on summoned mobs whose current target is null, dead, or no longer in the attacker set, leaving mobs actively engaging a live, in-set target alone. The append + nudge happen regardless of cooldown, so a second attacker during the 5-min summon-cooldown window still gets added to the pool and idle minions immediately swing onto them. Guards: `PlayerEntity` attackers are skipped (mobs never target players via this mechanism) and UUIDs already in `ownerByZombie` (the summoner's own summons) are skipped. Then, if not on cooldown, spawn **4 mobs per trigger** via `spawnZombies` → delegating to `spawnType` for each entity type: **2 zombies + 1 wither skeleton + 1 zombified piglin**. Each type gets its own 50-attempt positional spawn budget so a bad-position streak for one type doesn't starve the others. Then stamp `NECROMANCER_COOLDOWN_END` onto every worn Necromancer piece (for the client tooltip MM:SS countdown), play `ModSounds.NECROMANCER_SUMMON` on `SoundCategory.HOSTILE`, and instantly strip `ROTTEN_MUSCLE` off the summoner (so the HUD badge flips immediately rather than waiting for the 20s duration to tick out). Also handles the summoner-hits-own-zombie case: if the attacker is the victim's tracked owner, clear the victim's `setAttacker(null)` + `setTarget(null)` (predicate widened from `ZombieEntity` to `MobEntity` to cover all three summon types) so `RevengeGoal` doesn't latch the summoner — the predicate-gated `ActiveTargetGoal` installed at spawn then re-acquires a valid attacker-set entry on the next scan.
2. **`ServerLivingEntityEvents.ALLOW_DAMAGE`** — friendly-fire gate. Cancels damage (returns `false`) when the attacker is a tracked zombie AND the victim is that zombie's summoner. Summoner-hits-own-zombie path is NOT cancelled here (the summoner can kill their minions if they choose); the aggro-back is suppressed in the `AFTER_DAMAGE` path above. Load-bearing for blocked / 0-damage hits where `AFTER_DAMAGE` may not fire but `RevengeGoal` still latches.
3. **`ServerLivingEntityEvents.AFTER_DEATH`** — summoner-death cleanup. When a 4/4 wearer dies, iterate `ownerByZombie` and `discard()` every mob owned by the dying summoner, prune the per-mob tracking maps (`ownerByZombie`, `zombieExpireTick`), and drop the dying summoner's entry in `attackersBySummoner` (next summon cycle starts with a fresh attacker pool). Leaves `lastSummonTick` untouched (harmless without the set equipped, and the player doesn't have the set on respawn anyway).
4. **`ServerTickEvents.END_SERVER_TICK`** — lifetime cleanup (every 20 ticks). Walks `zombieExpireTick` and `discard()`s zombies past their 5-minute expiry, prunes `ownerByZombie` for any removed mob, and **drops the summoner's `attackersBySummoner` entry when their last alive owned mob is removed** so the attacker pool is reset for the next summon cycle. Entries for zombies in unloaded chunks are kept until their expire tick passes — pruning earlier would orphan `setPersistent` zombies whose chunk later reloads (letting a player ferry them into an unloaded area to permanently escape cleanup).

**Spawn loop** mirrors vanilla `ZombieEntity.damage`'s reinforcement mechanic — 50-attempt random-position loop per type, gated on `SpawnRestriction.isSpawnPosAllowed` + collision (`world.isSpaceEmpty` + `doesNotIntersectEntities`) — but scoped to a tight 3-block horizontal × 1-block vertical radius (vanilla's 7-40 is village-scale; here we want minions beside the wearer). The full `SpawnRestriction.canSpawn` predicate is **intentionally skipped** because it funnels through `HostileEntity.canSpawnInDark`, which fails at sky-light > 7 — i.e. daylight. A 4/4 set bonus must fire regardless of lighting, so position acceptance is ground + collision only. **Per-type budget**: each of the three entity types (`EntityType.ZOMBIE` ×2, `EntityType.WITHER_SKELETON` ×1, `EntityType.ZOMBIFIED_PIGLIN` ×1) runs its own 50-attempt loop via `spawnType` — so one type maxing out its rejects can't starve the others.

**Target locking** uses `MobEntityAccessor` (an `@Accessor` mixin widening `MobEntity.targetSelector`) to clear every `ActiveTargetGoal` from the spawned mob — otherwise vanilla's `ActiveTargetGoal<PlayerEntity>` rescans every tick and retargets the summoner (the nearest player). In their place, install a single predicate-gated `ActiveTargetGoal<LivingEntity>` whose filter reads `attackersBySummoner.get(summonerUuid)` live each tick and accepts any `LivingEntity` that is `isAlive()` and whose UUID is in the set; updating that map in `onAfterDamage` implicitly **grows the pool** of valid targets for every active mob owned by that summoner, so each summoner accumulates a **cumulative attacker list** of up to **16 hostiles** (FIFO eviction once the cap is hit, via the `LinkedHashSet` insertion-order iterator). `RevengeGoal` is intentionally left in place so non-summoner attackers still draw retaliation. Works uniformly across all three summon types because `lockTargetToAttacker` operates on `MobEntity` (the common supertype of `ZombieEntity` / `WitherSkeletonEntity` / `ZombifiedPiglinEntity`); tracking maps (`ownerByZombie`, `zombieExpireTick`) are intentionally **kept under their original zombie-flavored names to avoid churn** even though they now hold non-zombie mobs. **Attacker-set cleanup**: the `onEndTick` cleanup path removes the summoner's `attackersBySummoner` entry when the last alive mob owned by that summoner is discarded (death, lifetime expiry, or summoner death); the summoner-death `AFTER_DEATH` hook drops it eagerly as well.

**Equipment loadout** is built in `equipArmor`, **identical across all four summoned mobs regardless of type**: armor is hardcoded (Turtle Helmet + Leather Chestplate with Protection I + Leather Leggings + Leather Boots); the mainhand sword is **per-mob uniform RNG** chosen from a fixed 6-element `SWORD_TIERS` array — {WOODEN, STONE, IRON, GOLDEN, DIAMOND, NETHERITE} `_SWORD`, unenchanted — via `world.random.nextInt(SWORD_TIERS.length)`. Each of the 4 spawned mobs rolls independently, so a summon can produce e.g. one wooden-sword zombie + one netherite-sword wither skeleton. Drop chance is forced to 0.0 on every slot (armor + mainhand) via `setEquipmentDropChance(slot, 0.0f)` so kills never yield duplicate gear (keeps the summon mechanic from becoming a turtle-helmet or netherite-sword farm). The Protection enchant entry is fetched via `world.getRegistryManager().getOrThrow(RegistryKeys.ENCHANTMENT).getOrThrow(Enchantments.PROTECTION)` and attached via `ItemEnchantmentsComponent.Builder`.

Exposes a public helper `isOnCooldown(PlayerEntity, long worldTime)` consumed by `ArmorEffects`' `rotten_muscle_ready` bonus trigger — so the Rotten Muscle HUD badge gate and the summon gate share one source of truth (the `lastSummonTick` per-UUID map).

## Loot injection

**`loot/ModLootTableModifier.java`** — hooks Fabric's `LootTableEvents.MODIFY` and delegates to `LegendaryPool.buildFor`, `MapsPool.buildFor`, and `ThemedSetsPool.buildFor`, appending whichever pools they return to the vanilla chest's loot table. Source of truth for drop rates is the "Custom Loot Pool Allocation" Google Sheet.

**`loot/LegendaryPool.java`** — builds the legendary-items pool across four tiers (`CRAZY_TIER`, `S_TIER`, `A_TIER`, `F_TIER`) as `Set<RegistryKey<LootTable>>` of vanilla `LootTables` constants. Per-tier `emptyWeight`s: **Crazy=640, S=1440, A=1840, F=3040**. Item weights are 10 per legendary (×13) plus 30 for the weight-30 Mending book entry = **160** total item weight. With current empty weights that yields drop rates of ~20% / 10% / 8% / 5% (Crazy/S/A/F) — **the Google Sheet is authoritative if these drift**. The `namedItem(...)` helper in `LootPoolHelpers` is parallel to `buildItem` but uses loot functions (`SetNameLootFunction`, `SetLoreLootFunction`, `SetEnchantmentsLootFunction`) and supports enchantment **ranges** via `UniformLootNumberProvider`. The Mending book is a regular `ItemEntry.builder(Items.BOOK).weight(30)` entry — not guaranteed; it competes with the legendaries and the empty entry on the same weighted roll.

**`loot/MapsPool.java`** — builds a separate maps pool across the same four tiers. Three maps (jungle temple, ancient city, woodland mansion) each at weight 20 with per-tier empty weights **Crazy=5,999,940, S=5,999,940, A=340, F=540**, targeting overall map drop rates of ~0% / ~0% / 15% / 10%. Crazy/S empty weights are intentionally huge — map entries are kept structurally present so the sheet's tier schema stays uniform but effectively never roll. Ancient City map uses the custom `famspecial:on_ancient_city_maps` structure tag — the tag JSON lives at `src/main/resources/data/famspecial/tags/worldgen/structure/on_ancient_city_maps.json`; add a sibling file under the same path to support a new map destination. Non-overworld chests (End City, Nether Bridge, Bastions) are commented out of the map tier sets because `ExplorationMapLootFunction` searches in the chest's dimension and would silently produce blank maps.

**`loot/ThemedSetsPool.java`** — builds a themed-gear pool across the same four tiers as `LegendaryPool` (reuses `LegendaryPool.CRAZY_TIER` / `S_TIER` / `A_TIER` / `F_TIER` for the chest-id mapping). Injects **50 themed items** — 5 pieces from each of the 10 non-OldFam themes (Mistborn, Pacifist, Poseidon, FireSerpent, Necromancer, KnightRadiant, EshEndraNavesh, Shurima, Thor, RaidersLegacy) — each at **weight 10**, plus a Mending book at **weight 100**. Per-tier empty-slot weights: **Crazy=1900, S=4400, A=9400, F=19400**. Total item weight per tier = 50 × 10 + 100 = **600**. **OldFam is intentionally excluded** because OldFam items already drop via `LegendaryPool` and would otherwise double-roll. Mechanism: each themed item is wrapped in a `PrebuiltStackEntry` whose `Supplier<ItemStack>` calls the corresponding `build<ItemName>(RegistryWrapper<Enchantment>)` factory in the theme's `entries/*Items.java` — so the chest stack matches the creative-tab stack byte-for-byte (same components, same enchants, same custom name/lore). See "Per-item factory pattern" cross-ref in `ITEMS.md`'s entries-file descriptions; the `addWeapons` / `addTools` / `addArmor` lambdas now thinly delegate to those public factories.

**`loot/PrebuiltStackEntry.java`** — custom `LootPoolEntryType` registered as `famspecial:prebuilt_stack`. Subclass of `LeafEntry` that wraps a `Supplier<ItemStack>` and emits `supplier.get().copy()` on each roll, so every chest spawn is a fresh, independent stack (mutating one drop never aliases another). Registered via `PrebuiltStackEntry.register()` called from `FamSpecial#onInitialize`. Used exclusively by `ThemedSetsPool` to keep the chest output identical to the per-item factory output without re-implementing the build logic in loot-function form.

## `item/ArmorEffects.java`

Server tick listener (every 80 ticks = 4s, applying 400-tick / 20s effects). The 20s-duration-with-4s-refresh cadence re-ups before the sub-10s vanilla effect blink so buffs stay solid while worn and drop fast on unequip. Three independent mechanisms live here.

**Instant-apply dispatch via `refreshBonusesFor(ServerPlayerEntity player)`.** The body of what used to be the tick-loop iterator has been extracted into a public static helper `ArmorEffects.refreshBonusesFor(player)`: builds a `TickContext` for the player (reads `isNight` off the player's server world), walks `BONUSES`, applies effects, runs the `MOD_MANAGED` diff-eviction, and runs the Obsidian Dagger attribute swap. Idempotent and self-contained. The 80-tick tick loop now just iterates online players and calls `refreshBonusesFor(player)` on each — the slow path's behavior is unchanged and remains the eventual-consistency safety net. Three event-driven mixins call the same helper so the dispatch fires within one tick of the triggering event:

- `LivingEntityEquipMixin` (HEAD on `LivingEntity.onEquipStack`) — covers any armor-slot mutation that funnels through `equipStack` (standard wear, swap, death drop).
- `ServerPlayNetworkHandlerMixin` (TAIL on `onUpdateSelectedSlot`) — covers hotbar scroll / number-key swap.
- `ServerPlayNetworkHandlerClickSlotMixin` (TAIL on `onClickSlot` + `onCreativeInventoryAction`) — covers survival inventory drag / shift-click / armor-swap-key AND creative-tab drag. The creative hook is load-bearing because creative-tab armor drag does not route through `onClickSlot`; without it, HUD icons for armor-driven effects lagged up to 4 s in creative.

The refactor collapsed the mixins' previous hand-rolled enumerated strip lists (eight entries in `LivingEntityEquipMixin`, six in `ServerPlayNetworkHandlerMixin`) down to a single `refreshBonusesFor` call apiece — both APPLY and STRIP now flow through the MOD_MANAGED diff in one place. **Result**: six previously-effect-gated gameplay paths (`EMPERORS_DIVIDE`, `MESSMERS_FLAME_AEGIS`, `SUNS_PROTECTION`, `MESSMERS_VENOM`, `ASGARDIANS_FLIGHT`, `SHURIMAN_ENDURANCE`) activate within 1 tick of swap-in instead of up to 80; swap-out is equally instant via the same refresh call's diff pass.

Three independent mechanisms live in this file:

0. **Durability regen block** — a separate `% 243` cadence check runs *before* the `% 80` early return, so it ticks independently of the bonus-refresh path. For every online player × armor slot, if the equipped stack carries `REGENS_DURABILITY` and has non-zero damage, decrement its damage by 1. Used by the Knight Radiant Shard set to give it elytra-style self-repair.

1. **`BONUSES` dispatch loop** — a `List<Bonus>` where each `Bonus` carries a `Predicate<TickContext>` trigger and one-or-more `Effect`s (status effects), re-applied every tick while the trigger matches. `Bonus.effects(...)` is the only factory. `TickContext` bundles all six equipment slots + `isNight` + dimension, with helpers `hasFullSet(setId)` and `inTimelessDimension()` (Nether/End treated as always-dark for time-gated bonuses). Current entries:
   - `night_vision_helmet` — helmet has `GRANTS_NIGHT_VISION` → Night Vision I.
   - `mistborn_set_night` — all four armor slots share `SET_ID="mistborn"` **and** it's night or Nether/End → Strength I + Speed I. The hidden, time-gated Mistborn payoff.
   - `pacifist_full_set` — all four armor slots share `SET_ID="pacifist"` (no time gate) → Regeneration I.
   - `sages_grace_mainhand` — main-hand stack has `HEALS_TARGET` → Sage's Grace (cosmetic HUD badge; the actual heal-on-hit lives in `AttackHandlers`' `AttackEntityCallback` branch, gated on the same mainhand flag). No time gate. Applied with `showParticles=false` so the vanilla ambient bubbles don't radiate.
   - `water_breathing_helmet` — helmet has `GRANTS_WATER_BREATHING` → Water Breathing I.
   - `messmers_flame_aegis_chest` — chest has `GRANTS_MESSMERS_FLAME` → Messmer's Flame Aegis (read by `LivingEntityMixin` for the 80% fire-damage reduction).
   - `fire_serpent_full_set` — all four armor slots share `SET_ID="fire_serpent"` → Messmer's Venom (cosmetic aura on the wearer; `AttackHandlers` reads the effect to decide whether the wearer's hits propagate venom — weapon-agnostic, so fists, sword, axe, bow/arrow, snowball, anything carries it).
   - `healers_vision_helmet` — helmet has `SHOWS_ENTITY_HP` → Healer's Vision (cosmetic HUD badge; the actual HP-bar overlay lives in `HealthOverlay` and is driven by the same flag).
   - `poseidon_full_set` — all four armor slots share `SET_ID="poseidon"` → Empowered Riptide (cosmetic HUD badge; the actual dry-land riptide is gated in `TridentItemMixin`).
   - `night_strength_mainhand` — main-hand stack has `NIGHT_STRENGTH` **and** it's night or Nether/End → Night Strength (cosmetic HUD badge; the actual day/night damage swap happens in the main-hand block below). **Removed from `MOD_MANAGED`** — decays naturally over ~20 s after the gate drops rather than being diff-stripped within a 4 s cycle.
   - `tireless_lunge_mainhand` — main-hand stack has `NO_LUNGE_HUNGER` → Tireless Lunge (cosmetic HUD badge; the actual exhaustion-skip lives in `ApplyExhaustionEnchantmentEffectMixin`).
   - `stormlight_any_piece` — **any** of the four armor slots carries `REGENS_DURABILITY` → Stormlight (End Rod particle aura via the effect's `applyUpdateEffect`; a la carte, not 4/4-gated, matches the 243-tick durability-regen block's per-slot gate).
   - `shardbearing_mainhand` — main-hand stack has `GRANTS_SHARDBEARING` → Shardbearing (cosmetic HUD badge; the actual +10% max-HP chip lives in `LivingEntityMixin`'s `modifyAppliedDamage` handler and reads the same mainhand flag). No time gate.
   - `knight_radiant_full_set` — all four armor slots share `SET_ID="knight_radiant"` → Radiant Might (+8.0 ATTACK_DAMAGE via the effect's chained attribute modifier — between vanilla Strength II's +6.0 and Strength III's +9.0). No time gate. **Removed from `MOD_MANAGED`** — decays naturally over ~20 s after the set is broken rather than being diff-stripped within a 4 s cycle.
   - `rotten_muscle_ready` — all four armor slots share `SET_ID="necromancer"` **and** `NecromancerSummon.isOnCooldown(player, worldTime) == false` → Rotten Muscle HUD badge. Appears when the summon is charged, disappears during the 5-min cooldown window. Shares the `lastSummonTick` source of truth with the actual summon gate via `isOnCooldown`.
   - `undead_resistance_chest` — chest slot has `GRANTS_UNDEAD_RESISTANCE` → Undead Resistance HUD badge + green smoke aura; the 60% undead-damage reduction itself lives in `LivingEntityMixin`'s `famspecial$undeadResistance` ModifyReturnValue, gated on the same chest flag.
   - `wither_touch_mainhand` — main-hand stack has `APPLIES_WITHER_ON_HIT` → Wither Touch (cosmetic HUD badge; the Wither-IV-on-hit payload itself lives in `AttackHandlers`' `ALLOW_DAMAGE` branch, gated on the same mainhand flag — 8s vs mobs / 2s vs players, mob wither-kills drop a wither rose).
   - `ominous_boots` — boots have `GRANTS_OMINOUS` **and** it's night or Nether/End → vanilla `BAD_OMEN`. Lives on the Esh-Endra-Navesh boots (Dhoom Machale). Vanilla effect — deliberately left out of `MOD_MANAGED` so ambient Bad Omen sources (ominous-bottle, raid captain drops) decay naturally rather than being stripped on unequip.
   - `esh_endra_navesh_full_set` — all four armor slots share `SET_ID="esh_endra_navesh"` → vanilla `HASTE` at **amplifier 1 (Haste II)**; bumped from amplifier 0 / Haste I. No time gate. Same "vanilla effect, natural decay" convention as `mistborn_set_night` — left out of `MOD_MANAGED` so beacon/potion Haste isn't stripped on set swap.
   - `shadi_buff_mainhand` — main-hand stack has `BONUS_DIAMOND_CHANCE` → Shadi Buff (cosmetic HUD badge; the actual 1/700 bonus-diamond roll lives in `BlockBreakHandler`, gated on the same mainhand flag). No time gate.
   - `emperors_divide_mainhand` — main-hand stack has `IGNORES_KB_RESISTANCE` → Emperor's Divide. Lives on the Sun Disc Spear (Shurima). Gameplay: `LivingEntityKnockbackMixin` reads `attacker.hasStatusEffect(EMPERORS_DIVIDE)` and zeroes the victim's `KNOCKBACK_RESISTANCE` lookup so the `(1 - resist)` multiplier in `LivingEntity#takeKnockback` becomes 1 — knockback always lands full-strength regardless of target stiffness. Effect-presence and mainhand-flag share one source of truth: this bonus is the only path that applies the effect, and `LivingEntityKnockbackMixin` is the only consumer. No time gate.
   - `suns_protection_boots` — boots have `GRANTS_SUNS_PROTECTION` → Sun's Protection. Lives on Drifting Sands (Shurima boots). **Biome gate removed** — applies globally whenever the boots are worn (bonus id was renamed `suns_protection_boots_desert` → `suns_protection_boots` at the same time). Gameplay: `LivingEntityMixin`'s `modifyAppliedDamage` ModifyReturnValue scales incoming damage by 0.8× (Resistance-I-equivalent) when the victim has the effect. Removed from `MOD_MANAGED` — decays naturally over ~20 s after the trigger drops. **Shuriman Endurance (4/4) still gates on desert-family biome** — only Sun's Protection went global.
   - `shuriman_endurance_full_set_desert` — all four armor slots share `SET_ID="shurima"` **AND** the player is in a desert-family biome (same three-biome set as `suns_protection_boots_desert`) → Shuriman Endurance. The 4/4 Shurima set bonus. Gameplay: `PlayerEntityExhaustionMixin` HEAD-cancels `PlayerEntity#addExhaustion` whenever the player has the effect, so no saturation loss accrues from any source. **Beacon-style apply-once gating** — the dedicated branch in the dispatch loop applies the effect with a 4800-tick (4 min) duration and skips re-application while remaining > 4720 t (`SHURIMAN_ENDURANCE_REFRESH_THRESHOLD_TICKS`). The only `BONUSES` entry that does NOT use the standard 400-tick / 80-tick refresh cadence; load-bearing because without the apply-once gate, leaving the biome mid-buff would clobber the timer back to the 400-tick default and the effect would visibly snap to ~20 s. **Not in `MOD_MANAGED`** — decays naturally when the timer expires. Live call sites: `ArmorEffects.java:433-436` (server cache) and `FamSpecialClient.java:isInDesert()` (client tooltip gate).
   - `god_of_thunder_mainhand` — main-hand stack has `LIGHTNING_ON_HIT` → God of Thunder. Lives on Mjolnir. Cosmetic HUD badge advertising the per-hit lightning roll, which itself lives in `ThorEffects.AFTER_DAMAGE` gated on the same mainhand flag.
   - `storms_awakening_legs_ready` — leggings have `TRIGGERS_STORM_AWAKENING` **AND** `ThorEffects.isStormCooldown(player, worldTime) == false` → Storm's Awakening. Lives on Warrior's Greaves (Thor leggings). Cosmetic ability-armed indicator — disappears during the **24000-tick (1 MC day)** cooldown window after a successful trigger, re-appears once cleared. Shares the cooldown source of truth with the actual kill-trigger gate via `isStormCooldown`.
   - `asgardians_flight_full_thor_set_and_mjolnir` — all four armor slots share `SET_ID="thor"` **AND** main-hand stack has `THOR_MACE` → Asgardian's Flight. The **first** bonus that gates on an armor set AND a mainhand flag simultaneously — both halves of the predicate must hold. Drives Mjolnir's right-click riptide launch via the effect-presence check in `MjolnirMaceItem.onStoppedUsing` (rather than re-deriving the 5/5 gate at the call site), so the strip mixins on either half — `LivingEntityEquipMixin` (armor) and `ServerPlayNetworkHandlerMixin` (mainhand) — instantly disable the launch.
   - `bounty_hunter_legs` — leggings slot has `BOUNTY_HUNTER` → Bounty Hunter (cosmetic HUD badge; the actual 10%-on-arrow-or-firework-rocket-kill 1–3-diamond drop lives in `BountyHunterKills`, gated on the same leggings flag). Lives on Trousers of the Trail (Raider's Legacy legs). No time gate.
   - `technoblade_featherweight_full_set` — all four armor slots share `SET_ID="raider"` → applies `TECHNOBLADE_FEATHERWEIGHT`. **No cooldown gate** — always-on while the full set is worn. Drives fall-damage immunity via `LivingEntityMixin#famspecial$technobladeFallImmunity` (returns 0.0f when source is `DamageTypes.FALL` and victim has this effect).
   - `technoblade_never_dies_full_set` — all four armor slots share `SET_ID="raider"` **AND** `TechnobladeSave.isOnCooldown(player, worldTime) == false` → applies `TECHNOBLADE_NEVER_DIES`. **Charge-gated indicator that disappears during the 5-min cooldown window** (mirrors the Necromancer / Rotten Muscle pattern — same per-stack cooldown stamp is the source of truth, so icon and gameplay can't drift). Drives the totem-of-undying auto-save via `LivingEntityMixin#famspecial$technobladeTotemSave` which consumes a **5-min (6000 t)** per-set cooldown via `TechnobladeSave.tryConsume` when a hit would otherwise kill the wearer, applying the exact vanilla totem effect bundle + screen flash + sound (see the `TechnobladeSave` section below). The two `@ModifyReturnValue` injectors in `LivingEntityMixin` are declared **in order** (fall-immunity first, totem-save second) so the totem injector sees post-fall-zero values — a fatal fall is negated by the fall injector before the totem injector ever runs, so the cooldown isn't burned on falls. Splitting the previous single `technoblade_never_dies_full_set` bonus into these two entries is what makes the Technoblade Never Dies icon hide during cooldown while the Featherweight icon stays lit — tooltip code in `FamSpecialClient.java` is unchanged because `technoActive = hasEffect(TECHNOBLADE_NEVER_DIES) && cdRemaining <= 0L` naturally reads false during the cooldown window now that the effect isn't applied.
   - `crusaders_volley_mainhand` — main-hand stack has `CRUSADERS_VOLLEY` → Crusader's Volley (cosmetic HUD badge; the actual two-shots-per-charge scheduler lives in `FortuneGloryItem`, gated on the same mainhand flag). Lives on Fortune & Glory. No time gate.

   The apply loop passes `showParticles=false` for `ROTTEN_MUSCLE` and `WITHER_TOUCH` so the vanilla potion-ambient bubbles don't overlap the themed particles spawned by those effects' own `applyUpdateEffect` overrides (soul wisps / ash flakes). `UNDEAD_RESISTANCE` alone keeps `showParticles=true` since its green smoke is the visual cue for the piece-alone bonus. Adjust this branch if adding a new cosmetic effect that shouldn't radiate.

   The apply loop also tracks which mod-managed effects were active last tick per player via a `ConcurrentHashMap<UUID, Set<RegistryEntry<StatusEffect>>>`. On each 80-tick pass, any effect that was mod-applied last tick but isn't this tick gets explicitly `removeStatusEffect`'d — so the HUD icon disappears on the very next pass instead of lingering the full 20s refresh window. Only mod-registered effects (the `MOD_MANAGED` set, **20 entries**: `MESSMERS_FLAME_AEGIS`, `MESSMERS_VENOM`, `HEALERS_VISION`, `EMPOWERED_RIPTIDE`, `TIRELESS_LUNGE`, `STORMLIGHT`, `SHARDBEARING`, `ROTTEN_MUSCLE`, `UNDEAD_RESISTANCE`, `WITHER_TOUCH`, `SAGES_GRACE`, `SHADI_BUFF`, `EMPERORS_DIVIDE`, `GOD_OF_THUNDER`, `STORMS_AWAKENING`, `ASGARDIANS_FLIGHT`, `BOUNTY_HUNTER`, `TECHNOBLADE_FEATHERWEIGHT`, `TECHNOBLADE_NEVER_DIES`, `CRUSADERS_VOLLEY`) are eligible for explicit-remove. **Three custom effects are deliberately excluded and decay naturally** (~20 s after unequip rather than being diff-stripped within a 4 s cycle): `NIGHT_STRENGTH`, `RADIANT_MIGHT`, `SUNS_PROTECTION`. Vanilla effects applied by bonuses (`NIGHT_VISION`, `STRENGTH`, `SPEED`, `REGENERATION`, `WATER_BREATHING`, `BAD_OMEN`, `HASTE`) are also left to decay naturally so we don't strip a beacon or potion source the player legitimately has elsewhere. `SHURIMAN_ENDURANCE` is the one beacon-style custom effect (4800-t / 4-min apply-once) also excluded from `MOD_MANAGED` — it must decay naturally when its timer expires rather than being stripped on the next predicate flip. Per-player tracking is cleaned up on `ServerPlayConnectionEvents.DISCONNECT` and `ServerLivingEntityEvents.AFTER_DEATH` so the map doesn't grow unbounded and a dead/reconnected player doesn't carry stale state.

2. **Main-hand attribute-modifier swap** — for stacks carrying `NIGHT_STRENGTH`, swaps between `MistbornItems.DAGGER_DAY_MODIFIERS` and `DAGGER_NIGHT_MODIFIERS` on the held stack so the **tooltip damage numbers themselves change** day/night. Only the main-hand copy is touched.

Also exposes a public helper `hasFullSet(PlayerEntity, String setId)` — used outside the tick loop by `TridentItemMixin` (Empowered Riptide gate) and `FamSpecialClient` tooltip callbacks. It mirrors `TickContext.hasFullSet` but reads live off the player instead of pre-fetched stacks.

## `item/AttackHandlers.java`

Registers two separate hooks because Fabric's two event channels cover different damage paths:

1. **`ServerLivingEntityEvents.ALLOW_DAMAGE`** (pre-damage, fires for *every* damage path since they all funnel through `LivingEntity.damage`) — two additive branches. (a) If the attacker is a player with `MESSMERS_VENOM` and the victim is not the attacker, apply Messmer's Venom (124 t / 6.2 s, amp 0) to the victim. **Weapon-agnostic** — any successful damage path carries the venom: fists, sword, axe, bow/arrow, snowball, anything. Set membership is read indirectly through the aura (`MESSMERS_VENOM` is granted by `ArmorEffects`' `fire_serpent_full_set` bonus while 4/4 is worn), so the `hasStatusEffect` check IS the full-set check. `ALLOW_DAMAGE` catches every path because every damage path funnels through `LivingEntity.damage` (`AttackEntityCallback` would miss lunges and projectile hits). The 124-tick duration is intentional: its non-multiple-of-25 alignment offsets the first venom tick past the application's 10-tick i-frame window, so all 4 hits (at duration 100 / 75 / 50 / 25) land at full strength — see the constant's comment block at `AttackHandlers.java:36-47` for the full rationale. (b) **Thriller's Edge wither-on-hit branch.** If the attacker is a player whose **main-hand** stack carries `APPLIES_WITHER_ON_HIT`, apply **Wither IV** to the victim — **8s vs mobs / 2s vs players** (PvE/PvP duration split mirroring Messmer's Venom). Checked against `player.getMainHandStack()` rather than `source.getWeaponStack()` so off-hand flagged stacks don't propagate — matches the axe's `AttributeModifierSlot.MAINHAND`-gated attack-speed modifier. Mob victims (never players) are added to an `AttackHandlers`-scoped `Set<UUID>`; a sibling `ServerLivingEntityEvents.AFTER_DEATH` hook drops a `WITHER_ROSE` at the death position when a tagged mob dies to `DamageTypes.WITHER` and removes the UUID (one rose per death). A 200-tick (10 s) `END_SERVER_TICK` sweep prunes UUIDs whose entities are no longer loaded across any of the server's worlds. Always returns `true` — both branches are additive propagation, not cancellations.

2. **`AttackEntityCallback.EVENT`** (pre-damage, left-click melee only — fires inside `PlayerEntity.attack` and misses lunge dashes + projectile hits) — if the attacker's main-hand stack has `HEALS_TARGET`, replicate vanilla's private `isCriticalHit` check + cooldown gate, heal the target (if `LivingEntity`) by 4 HP on a normal hit or 8 HP on a crit, spawn heart particles, play the vanilla attack-sound (STRONG/WEAK/CRIT), consume 3 durability via `stack.damage(...)` (since vanilla's wear path is short-circuited by our cancel), reset the attack cooldown, and return `ActionResult.SUCCESS` to cancel vanilla damage/knockback/sweep/durability. Side effects (heal, sounds, particles, durability) are gated to `ServerWorld` to avoid desync and double-broadcast; the cooldown reset + cancel return run on both sides. Sweep is implicitly defeated — `AttackEntityCallback` never fires for sweep-caught entities, and `SUCCESS` short-circuits `PlayerEntity.attack` before `doSweepingAttack` runs.

Pattern choice: use **`AttackEntityCallback`** for behavior that must *replace* the damage/knockback (heal, freeze, teleport, etc.); use **`ALLOW_DAMAGE`** for behavior that runs *additively* alongside normal damage and needs to cover non-melee paths too (debuffs, bleed, lifesteal).

## `item/BlockBreakHandler.java`

Bonus-diamond drops for `BONUS_DIAMOND_CHANCE`-flagged tools (currently just Just Hit Bro, the Esh-Endra-Navesh pickaxe). Registers one hook on `PlayerBlockBreakEvents.AFTER`, which fires post-break — the block has already been consumed and its own drops have spawned, so the handler layers phantom diamond drops on top rather than replacing anything.

Guards: `ServerWorld` only (AFTER fires client-side too for prediction), server-side `ServerPlayerEntity` only, creative/spectator excluded. Then reads `BONUS_DIAMOND_CHANCE` off the player's **main-hand** stack — so the pickaxe must be the tool that actually broke the block, not just present in the inventory.

Roll: `serverWorld.random.nextInt(700) != 0` early-returns — 1/700 per break. On success, `Block.dropStacks(Blocks.DIAMOND_ORE.getDefaultState(), world, pos, blockEntity=null, player, tool)` runs vanilla's `blocks/diamond_ore` loot table verbatim with the same tool the player used, so **Fortune rolls the raw-diamond multiplier and Silk Touch drops a diamond_ore block** exactly as they would against a real ore break. Then plays `ModSounds.BONUS_DIAMOND` at the block position on `SoundCategory.PLAYERS` (rides the Players volume slider — this is gear-event, not a world-ambient sound), `source=null` so nearby clients also hear the proc.

The roll probability is the single tuning knob, stored as `BONUS_DIAMOND_ROLL_DENOMINATOR` at top of file. Adjust there, not at each call site.

## `item/BountyHunterKills.java`

Bounty Hunter — the leggings-gated arrow-or-firework-rocket-kill bonus drop for `BOUNTY_HUNTER`-flagged Raider's Legacy legs (Trousers of the Trail). Event-driven via `ServerLivingEntityEvents.AFTER_DEATH`, registered from `onInitialize`. Server-side only.

Predicate, evaluated in order: (a) the killer is a `ServerPlayerEntity`; (b) the player's leggings carry `BOUNTY_HUNTER`; (c) the damage source's projectile is an `ArrowEntity` **or** a `FireworkRocketEntity`. Wearer attribution is projectile-agnostic — `damageSource.getAttacker()` resolves to the player who shot the firework rocket (the crossbow's owner), so the same leggings-slot inspection works for both projectile types. The predicate naturally includes spectral arrows (subclass `ArrowEntity`). Other projectile classes (tridents, snowballs, eggs) don't match. On a match, roll **10%** (`DROP_CHANCE = 0.10f`, bumped from `0.05f`); on success, drop **1–3 diamonds** at the corpse position via `Block.dropStack` and play `ModSounds.INDY` at the kill position. The HUD-badge advertisement lives in `ArmorEffects`' `bounty_hunter_legs` bonus on the same leggings flag so badge + mechanic share one source of truth.

## `item/TechnobladeSave.java`

Technoblade Never Dies' **totem-of-undying auto-save** half. The fall-damage immunity half is `LivingEntityMixin`'s `famspecial$technobladeFallImmunity` injector and is unchanged. This file is a static-utility class (`private TechnobladeSave() {}`, no `register()`) sibling to `ThorEffects` / `NecromancerSummon` in shape — it's called directly from `LivingEntityMixin`'s second Raider injector rather than registering its own event listener. No subsystem entry in `FamSpecial.onInitialize` is needed.

**`tryConsume(PlayerEntity, DamageSource)`** — the entry point invoked by `LivingEntityMixin#famspecial$technobladeTotemSave` when an incoming hit would otherwise kill the wearer (`original >= self.getHealth() + self.getAbsorptionAmount()`). Predicate (all must hold, all guards short-circuit to `false`): (1) the player's world is a `ServerWorld`; (2) the player has `TECHNOBLADE_NEVER_DIES` (effect-presence is the gate for the totem-save half; fall immunity gates on `TECHNOBLADE_FEATHERWEIGHT` instead); (3) `isOnCooldown` returns false (the 5-min totem cooldown is clear).

On success: apply the **exact vanilla totem effect bundle** — `Regeneration` amp 1 / 900 t (45 s), `Fire Resistance` amp 0 / 800 t (40 s), `Absorption` amp 1 / 100 t (5 s) — durations match `Items.TOTEM_OF_UNDYING`'s `onConsume` path so the save feels indistinguishable from a real totem proc. Then `player.setHealth(1.0f)` clamps to 1 HP so the caller's `return totalHP - 1.0f` damage-cap doesn't underflow against a fractional `getHealth` from prior damage math. Stamp the cooldown end tick (`worldTime + COOLDOWN_TICKS` where `COOLDOWN_TICKS = 6000L` — **5 minutes (6000 ticks) at 20 tps**, shortened from the earlier 30-minute / 36000-tick value) onto every worn Raider piece via the `TECHNOBLADE_TOTEM_COOLDOWN_END` component — mirrors `NecromancerSummon.stampCooldownOnWornSet`. Then emit client FX byte-for-byte identical to vanilla totem: `world.sendEntityStatus(player, EntityStatuses.USE_TOTEM_OF_UNDYING)` (entity status `35` — replicates the gold screen flash + heart particle burst to every client tracking the player) and `world.playSound(null, x, y, z, SoundEvents.ITEM_TOTEM_USE, SoundCategory.PLAYERS, 1.0f, 1.0f)` (null source so every nearby player hears the proc — matches vanilla `PlayerEntity#tryUseTotem`). Returns `true`.

**`isOnCooldown(PlayerEntity, long worldTime)`** — read-only probe. Scans all 4 armor slots for Raider pieces (`SET_ID="raider"`) and returns the max-stamped `TECHNOBLADE_TOTEM_COOLDOWN_END` value; `true` iff `max > worldTime`. **The stamp itself is the source of truth** — unlike `ThorEffects` / `NecromancerSummon`, there is no parallel per-UUID `lastConsumeTick` map. Rationale: the cooldown lives on the gear, so respawning into a fresh inventory loses the stamp (no exploit — the user must re-earn the set), but the stamp survives chestplate→elytra anvil merge (the new component is in `MOD_COMPONENTS_BASE`) and any other component-copy path. Returns `false` when no piece carries a stamp (fresh set never triggered) or every stamp has elapsed (the stamp stays on the gear post-expiry — the tooltip just hides once `currentTime >= value`).

**Edge cases worth knowing.** `/kill` and `DamageTypes.OUT_OF_WORLD` bypass `LivingEntity.modifyAppliedDamage` entirely, so the totem injector never runs and the save doesn't fire (matches vanilla totem behavior). Fall damage is zero'd by the fall-immunity injector (declared FIRST in `LivingEntityMixin`) before the totem injector sees the value, so a fatal fall never burns the cooldown — the declaration-order constraint between the two injectors is load-bearing for this no-double-consume guarantee.

## `item/MjolnirMaceItem.java`

`MaceItem` subclass backing Mjolnir, the Thor signature weapon (and 5th piece of the 5/5 Thor set). Right-click on the main hand starts a `UseAction.SPEAR` charge for `CHARGE_TICKS` (10), then `onStoppedUsing` launches the player along their look vector with vanilla `TridentItem`'s Riptide math. Launch magnitude is looked up dynamically via `EnchantmentHelper.getTridentSpinAttackStrength(stack, player)` at `MjolnirMaceItem.java:74` — the same call vanilla `TridentItem` uses — so the launch **scales with the Riptide enchant level on the stack**; this is **not cosmetic/visual flavor** as earlier docs claimed. Mjolnir ships with **Riptide III** (the post-datapack-override max from `data/minecraft/enchantment/riptide.json`'s `max_level: 3`); see the datapack tag overrides section below for how Mjolnir is opted into Riptide via the custom `famspecial:riptide_eligible` tag. No water, no rain, no Riptide enchant required to trigger (level 0 still launches — the Riptide level only tunes magnitude). The launch is gated three ways: hand must be `MAIN_HAND`, the stack must carry `THOR_MACE`, and the player must currently have `ASGARDIANS_FLIGHT` — any failure short-circuits to `super.use` / `super.onStoppedUsing` and the mace behaves like a vanilla mace.

**Asgardian's Flight as gameplay predicate.** `MjolnirMaceItem` reads `player.hasStatusEffect(ASGARDIANS_FLIGHT)` rather than re-deriving the 5/5 + mainhand gate at the call site. The effect is the source of truth — applied by `ArmorEffects`' `asgardians_flight_full_thor_set_and_mjolnir` bonus and instant-stripped via `ArmorEffects.refreshBonusesFor(player)` called from the equip / slot / click-slot mixins when either half drops. Decoupling the gate this way means a mid-charge armor/hotbar/inventory swap drops the launch on the next tick without `MjolnirMaceItem` having to re-walk the equipment slots.

**Client-authoritative velocity.** `onStoppedUsing` mirrors vanilla `TridentItem` byte-for-byte — no `if (world.isClient()) return true;` early exit. The launch math must run on **both** client and server: `LocalPlayerEntity` is client-authoritative on its own velocity, so a server-only `addVelocity` becomes an `EntityVelocityUpdateS2CPacket` that the local player ignores, producing spin-without-launch (the v1.2.5 bug). Do not re-introduce the client-side early return. Only the cooldown arming (`getItemCooldownManager().set(stack, RIPTIDE_SPIN_TICKS)`) is server-only — the client receives that via the cooldown packet on the next tick.

**Per-launch durability cost.** Every successful launch — whether it's the vanilla water/rain riptide path or Asgardian's Flight's dry-land launch — costs **−1 durability** on Mjolnir. Applied server-side at `MjolnirMaceItem.java:91` via `stack.damage(1, player, EquipmentSlot.MAINHAND)`, after the cooldown is set and immediately before `onStoppedUsing` returns. Gated on `!world.isClient()` so the wear is authoritative from the server and not double-counted on the client. Too-short charges (below `CHARGE_TICKS`) are early-returned via `super.onStoppedUsing` at `MjolnirMaceItem.java:61` and **do not** incur the cost — durability is only consumed when the launch actually fires.

## `item/FortuneGloryItem.java`

`CrossbowItem` subclass backing **Fortune & Glory**, the Raider's Legacy signature crossbow. Sibling to `MjolnirMaceItem` in the signature-weapon-subclass discipline: **must be registered in `RaidersLegacyItems.register()` from `FamSpecial.onInitialize` before `ModItemGroups.registerItemGroups()` runs** — registering an `Item` subclass at static-final class-load crashes in 1.21+ because `SimpleRegistry` rejects intrusive holders during the early-boot window the creative-tab-entries lambda runs in.

**Three-shot scheduler — Crusader's Volley.** A single right-click charge fires **three shots back-to-back** at a snappy ~2-tick cadence (~100 ms gap between each, ~200 ms total spread), with all three shots matching the originally-loaded projectile type (arrows OR firework rockets). The `use` override calls `super.use(...)` first so vanilla fires shot 1 exactly as it would for any crossbow, then — gated on the stack carrying `CRUSADERS_VOLLEY` and the user being a `ServerPlayerEntity` — appends **two pending shots** into a private `ConcurrentLinkedQueue<ScheduledShot>` (no per-player keying — `ScheduledShot` carries the player UUID directly). The loaded projectile is captured from the stack's `CHARGED_PROJECTILES` component **before** `super.use(...)` clears it and stashed on the `ScheduledShot` record so shots 2 and 3 can replay the right entity type even though the component is gone by the time they fire. Constants: `SHOT_2_DELAY_TICKS = 2`, `SHOT_3_DELAY_TICKS = 4`, so shot 1 at t=0, shot 2 at t=2, shot 3 at t=4. A `ServerTickEvents.END_SERVER_TICK` handler (registered from `FortuneGloryItem.registerTickHandler()`, wired from `FamSpecial.onInitialize`) drains the queue every server tick: any entry whose target tick has elapsed fires via `fireScheduledShot`, which branches on the captured projectile — `FireworkRocketItem` → spawn a `FireworkRocketEntity` matching vanilla `CrossbowItem`'s firework path; otherwise → spawn a `VolleyArrowEntity` (the iframe-bypass arrow path described below).

**Iframe bypass — unified ALLOW_DAMAGE hook keyed off a UUID set.** The 2-tick cadence is below vanilla's `LivingEntity.timeUntilRegen` 10-tick invulnerability window, so shots 2 and 3 would normally be silently rejected as equal-or-lesser-damage during iframes. The bypass is now a single mechanism that covers **both** arrows and firework rockets: `FortuneGloryItem` maintains a class-scoped `Set<UUID> FG_PROJECTILE_UUIDS` (`ConcurrentHashMap.newKeySet()`); `fireScheduledShot` stamps each `VolleyArrowEntity` AND each `FireworkRocketEntity` it spawns via the volley path by calling `FG_PROJECTILE_UUIDS.add(projectile.getUuid())`. A `ServerLivingEntityEvents.ALLOW_DAMAGE` listener (registered from `FortuneGloryItem.registerTickHandler`) reads `source.getSource().getUuid()` on every damage event; if the UUID is in the set, it sets `victim.timeUntilRegen = 0` before returning `true` so vanilla's downstream damage path sees a clean iframe state. Shot 1 fires via `super.use` (vanilla) and is intentionally **not tracked** — it lands the first hit before any iframes exist. A 20-tick (`now % 20L == 0L`) cleanup sweep inside the same tick handler purges UUIDs whose entities are no longer loaded (`overworld.getEntity(uuid) == null`), so the set never leaks across pierces / despawns. **Net effect**: firework volley shots 2 and 3 now stack damage on the same target (previously absorbed by iframes); arrow volley shots 2 and 3 still bypass iframes, just via the new mechanism instead of `VolleyArrowEntity.onEntityHit`. Everything else about hit behavior (Multishot/Piercing inheritance, damage scaling, knockback) stays vanilla.

**Multishot/Piercing inheritance.** Shots 2 and 3 read the source crossbow stack's enchantment levels at fire time (from the stashed stack snapshot) and inherit Multishot and Piercing exactly as vanilla would for a fresh shot — so **Multishot III + Crusader's Volley = 9 arrows per release**. This is intentional per the user's design — the volley-style mechanic stacks multiplicatively with the crossbow's own enchant pool, not additively. Note: a companion enchant-block carve-out makes vanilla means of applying Multishot to Fortune & Glory unavailable; the inheritance code is defensive, kept in for creative `/enchant` and NBT-tool paths.

**Ammo / durability discipline.** "Single reload, triple shot." Shots 2 and 3 do **not** consume additional projectiles from the player's inventory and do **not** wear the crossbow's durability beyond what vanilla took for shot 1 — the queue-drain just re-fires the cached arrow path against the world without re-running vanilla's ammo-consume / wear-tick code. `Stats.USED.getOrCreateStat(Items.CROSSBOW)` is incremented once per shot (3 increments per release) so the player's stats screen reflects the true shot count. If the user disconnects or dies before the cadence completes, pending entries for that UUID are removed via the disconnect / `AFTER_DEATH` listeners.

The HUD-badge advertisement lives in `ArmorEffects`' `crusaders_volley_mainhand` bonus on the same `CRUSADERS_VOLLEY` flag so badge + mechanic share one source of truth.

**Anvil merge (cross-reference).** F&G can be repaired and enchant-merged in an anvil with another F&G or with a vanilla `Items.CROSSBOW` in either slot — implemented as a sibling HEAD-cancellable inject `famspecial$mergeFortuneAndGlory` in `AnvilScreenHandlerMixin` (see `MIXINS.md`). Result is always F&G via `fgSource.copy()`; Multishot is stripped on the result via `Enchantment.isAcceptableItem` (mirroring the enchanting-table block driven by the `#famspecial:multishot_eligible` tag override above); Quick Charge IV is preserved as a floor. Modded crossbows (non-vanilla, non-F&G) fall through to vanilla untouched — the gate is an exact `Items.CROSSBOW` identity check, not `instanceof CrossbowItem`.

## Datapack tag overrides (Mjolnir enchant eligibility)

The mod ships a small set of resource-pack JSONs under `src/main/resources/data/` that opt **Mjolnir** into a curated subset of vanilla enchantment pools. This is a **runtime-data mechanism, not a mixin** — no code changes are involved; the JSONs ship with the jar and Mojang's data loader merges them with the vanilla tags at load time.

**Why tag overrides instead of a mixin.** Vanilla `Enchantment#isAcceptableItem(stack)` checks the enchantment's `supported_items` tag against the **registered item id**. Mjolnir is registered as `famspecial:mjolnir`, not `minecraft:mace`, so it falls outside any vanilla tag that lists items by id (`minecraft:enchantable/mace`, `minecraft:enchantable/weapon`, etc.). Tag overrides are the cleanest way to opt a custom-id item into specific vanilla enchant pools without touching vanilla code or layering a mixin on `isAcceptableItem`.

**The vanilla `enchantable/*` tag overrides** — all under `src/main/resources/data/minecraft/tags/item/enchantable/`, all using `{"replace": false, "values": [...]}` so they're additive to vanilla:

- `durability.json` — opts **`famspecial:mjolnir`** and **`famspecial:fortune_and_glory`** into **Mending / Unbreaking / Curse of Vanishing**.
- `mace.json` — opts Mjolnir into **Density / Breach / Wind Burst** (the mace-specific pool).
- `weapon.json` — opts Mjolnir into **Smite / Bane of Arthropods**. Sharpness is intentionally **not** enabled — it gates on `minecraft:enchantable/sharp_weapon`, which Mjolnir is deliberately left out of.
- `fire_aspect.json` — opts Mjolnir into **Fire Aspect**.
- `crossbow.json` — opts **`famspecial:fortune_and_glory`** into **Quick Charge / Multishot / Piercing** (the crossbow-specific pool). Same additive-override mechanism as the Mjolnir entries above. **Multishot is then gated back out** by the targeted Multishot override below — Fortune & Glory still appears in this tag, but the Multishot enchantment no longer reads from this tag (see "The Multishot-exclusion carve-out" below).

**The Riptide-only carve-out** uses two files (already shipped before this change):

- `src/main/resources/data/famspecial/tags/item/riptide_eligible.json` — a custom item tag whose value is `["#minecraft:enchantable/trident", "famspecial:mjolnir"]`, i.e. the union of vanilla trident-enchantables and Mjolnir.
- `src/main/resources/data/minecraft/enchantment/riptide.json` — a vanilla-enchantment **override** that copies the rest of vanilla Riptide verbatim (anvil_cost, effects, exclusive_set, max_level, etc.) but swaps `supported_items` from `#minecraft:enchantable/trident` to `#famspecial:riptide_eligible`. Adding Mjolnir straight to `enchantable/trident` would also opt it into Loyalty + Channeling, which we explicitly don't want — overriding Riptide alone is the only enchant we want to extend.

**The Multishot-exclusion carve-out** (Fortune & Glory) is the mirror image of the Mjolnir/Riptide pattern — instead of opting a custom item *into* a vanilla enchant, it gates a vanilla item *out of* one. **Why:** the previous "6-arrow chaos" intent for Fortune & Glory is being walked back; Multishot is no longer wanted on the crossbow. **What:**

- `src/main/resources/data/famspecial/tags/item/multishot_eligible.json` — a custom item tag whose value is `["minecraft:crossbow"]` only. Fortune & Glory is intentionally **absent**.
- `src/main/resources/data/minecraft/enchantment/multishot.json` — a vanilla-enchantment **override** that copies vanilla Multishot byte-for-byte (anvil_cost, effects, exclusive_set, max_cost, max_level, min_cost, slots, weight, description) but swaps `supported_items` from `#minecraft:enchantable/crossbow` to `#famspecial:multishot_eligible`. Because the new tag omits `famspecial:fortune_and_glory`, Multishot's `isAcceptableItem` check now returns false for the custom crossbow.

**The arrangement.** All other crossbow enchants (Quick Charge, Piercing) continue to read from the unmodified `#minecraft:enchantable/crossbow` tag, which Fortune & Glory still belongs to via the additive `crossbow.json` override above; durability enchants (Mending, Unbreaking, Curse of Vanishing) still come from the additive `durability.json` override. Only Multishot now reads from the more restrictive `#famspecial:multishot_eligible` tag. Vanilla `minecraft:crossbow` is unaffected — it's listed in the new tag, so it still receives Multishot normally. The parallel multishot-inheritance code in `FortuneGloryItem.fireScheduledShot` (the second-shot fire path) is left in as defensive — fires correctly if a creative-mode `/enchant` command bypasses the data layer.

**Net effect: Fortune & Glory = all vanilla crossbow enchants except Multishot.** Accepts Quick Charge, Piercing, Mending, Unbreaking, Curse of Vanishing. **Does not** accept Multishot.

**Net effect: Mjolnir = vanilla mace enchants + Riptide only.** Accepts Density / Breach / Wind Burst, Smite / Bane, Fire Aspect, Mending / Unbreaking / Curse of Vanishing, and Riptide. **Does not** accept Sharpness, Loyalty, or Channeling.

**When to reach for this technique.** Any time a custom-id item should accept a vanilla enchant the vanilla tag doesn't list it under. To opt into an entire vanilla enchant pool, drop a `{"replace": false, "values": [...]}` JSON at `data/minecraft/tags/item/enchantable/<pool>.json`. To accept an individual enchant whose pool you don't want to fully extend, define a custom item tag under `data/<modid>/tags/item/<name>.json` and override the vanilla enchantment JSON at `data/minecraft/enchantment/<enchant>.json` with `supported_items` pointing at your custom tag. No mixin, no code wiring — rebuild and ship.

## Custom-id item parity for vanilla advancements/stats

Same root cause as the enchant-tag overrides above — `famspecial:mjolnir` and `famspecial:fortune_and_glory` aren't `minecraft:mace` / `minecraft:crossbow` by id, so vanilla advancement criteria that pin on those item ids (the four crossbow advancements `ol_betsy` / `arbalistic` / `two_birds_one_arrow` / `whos_the_pillager_now`, plus the mace advancement `overoverkill`) won't trigger when the custom-id items are used, and vanilla per-item stats slots (`Stats.USED.getOrCreateStat(Items.CROSSBOW/MACE)`) won't increment either. Fixed in two coordinated places: (1) two custom item tags `data/famspecial/tags/item/counts_as_crossbow.json` and `counts_as_mace.json` define the union of vanilla id + custom id, then five vanilla advancement JSONs are overridden under `data/minecraft/advancement/adventure/` byte-for-byte except for the single `items` field swapped to `#famspecial:counts_as_crossbow` or `#famspecial:counts_as_mace`; (2) `MjolnirMaceItem#onStoppedUsing` and both `FortuneGloryItem#use` (first shot path) and `FortuneGloryItem#fireSecondShot` manually fire `Stats.USED.getOrCreateStat(Items.MACE/CROSSBOW)` on top of the vanilla-counterpart stat slot the `MaceItem` / `CrossbowItem` superclasses already increment for the custom id, so the player's stats screen shows non-zero entries for **both** the custom and vanilla counterparts. **Future custom-id items that should satisfy a vanilla advancement / increment a vanilla stat should follow the same `counts_as_<vanilla_id>` tag convention plus a manual `Stats.USED` increment in the use/release path.**

## Chestplate→elytra anvil merge (attribute-modifier trade-off)

Cross-references the merge implementation in `AnvilScreenHandlerMixin` (`MIXINS.md`). Two SYSTEMS-relevant side effects:

1. **Armor points traded for flight.** `famspecial$buildMergedElytra` filters the source chestplate's `ATTRIBUTE_MODIFIERS` component before copying it onto the elytra output: entries whose attribute is `EntityAttributes.ARMOR` or `EntityAttributes.ARMOR_TOUGHNESS` are skipped, but every other modifier (MAX_HEALTH, MOVEMENT_SPEED, KNOCKBACK_RESISTANCE, any future custom attribute) is copied through. Net effect: the merged elytra trades the chestplate's diamond-baseline armor points for flight while retaining the per-theme stat bonuses.
2. **Custom-component transplant.** Mod components are carried via `ModComponents.copyModComponentsForElytra(source, out)` — see the `ModComponents` section for the `MOD_COMPONENTS_BASE` / `ELYTRA_EXCLUDED_COMPONENTS` split.

Identity-gating and the Mending/Unbreaking enchant merge (both inputs contribute; max level wins on duplicates) live in the mixin itself; see `MIXINS.md` for the merge eligibility predicate (`MERGE_ELIGIBLE_SETS` + `IS_FAMSPECIAL_GEAR` forgery gate) and the enchant filter.

**Thor armor MAX_HEALTH cross-reference.** The 4 Thor armor pieces (Thunderhelm, Asgardian Plate, Warrior's Greaves, Stormstrider) each carry **+1 MAX_HEALTH** via `ATTRIBUTE_MODIFIERS` components built in `ThorItems.buildThorArmor` (`ThorItems.java:117` onward; per-piece constants `HELM_MODIFIERS` / `PLATE_MODIFIERS` / `GREAVES_MODIFIERS` / `STRIDER_MODIFIERS` at lines 112–115). A full 4-piece Thor armor set therefore adds **+2 hearts**. The bonus carries through the chestplate→elytra merge because `MAX_HEALTH` is not in the attribute-filter skip list above — so a Thor-chestplate-merged elytra keeps its +1 HP contribution even though the diamond armor/toughness baseline is stripped. Primary per-piece documentation lives in `ITEMS.md`; this cross-reference exists so SYSTEMS readers tracing the merge's attribute behavior can trace the Thor bonus to its source.

## `item/ThorEffects.java`

Event-driven Thor handlers — Mjolnir's lightning-on-hit roll plus Warrior's Greaves' Storm's Awakening kill trigger. Two listeners registered from `onInitialize`:

1. **`ServerLivingEntityEvents.AFTER_DAMAGE`** — Mjolnir lightning. When the attacker is a player whose **main-hand** stack carries `LIGHTNING_ON_HIT` (Mjolnir), roll a per-hit chance to spawn a `LightningEntity` at the victim — 100% during a thunderstorm, 20% in clear weather. Mirrors the `god_of_thunder_mainhand` HUD-badge gate so the badge truthfully reflects when the mechanic is armed. **Damage profile**: the spawned bolt is flagged `setCosmetic(true)` so vanilla skips its own 5.0-damage / 8 s on-fire / block-ignition pass, and we instead apply **`LIGHTNING_DAMAGE = 100.0F` (50 hearts)** directly via `victim.damage(world, world.getDamageSources().lightningBolt(), 100.0F)` and ignite the target for `LIGHTNING_FIRE_SECONDS = 8` via `setOnFireFor(8)`. Using the `DamageTypes.LIGHTNING_BOLT` source keeps armor / Protection enchant / status-effect resolution behaving exactly like a real lightning hit. Visual and thunder sound replicate to all clients identically regardless of the cosmetic flag — that flag gates only server-side damage/fire/ignition logic. Channeler attribution is preserved (`bolt.setChanneler(sp)` before spawn), so Channeling-enchant interactions and kill-attribution are unchanged. **Trade-off**: cosmetic bolts do not light the block under the strike (vanilla lightning sometimes does), and the wearer no longer takes incidental self-damage if standing in the strike radius — both side effects are gone because vanilla's onTick damage/ignition pass is skipped.
2. **`ServerLivingEntityEvents.AFTER_DEATH`** — Storm's Awakening. When the killer is a player wearing Warrior's Greaves (leggings flagged `TRIGGERS_STORM_AWAKENING`) and the per-player cooldown is clear, roll the trigger chance — **`STORM_TRIGGER_CHANCE = 0.08f`** (flat 8% per kill, at `ThorEffects.java:54`; this is the production value, down from the `1.00f` temporarily used during testing) — to start a thunderstorm. **If `world.isThundering()` is already true, skip the kill entirely — no roll, no cooldown burn.** On a successful trigger, play `SoundEvents.ENTITY_LIGHTNING_BOLT_THUNDER` on `SoundCategory.WEATHER` at the killer's position, **vol 10000, pitch 0.8**, immediately before `world.setWeather(0, STORM_THUNDER_DURATION_TICKS, true, true)` — **`STORM_THUNDER_DURATION_TICKS = 2400`** (~2 min real-time, at `ThorEffects.java:60`; down from the earlier 4800-tick / ~4 min value). The vol-10000 hack abuses vanilla's volume-to-attenuation-range scaling so the thunderclap reaches every player in the world (not just nearby) — this is the audible "the storm answered" cue. On a successful trigger, `STORM_COOLDOWN_END` is also stamped onto the worn Warrior's Greaves via the same pattern as `NECROMANCER_COOLDOWN_END`. Cooldown only arms on a successful trigger and is per-player (UUID → world-tick map), **24000 ticks (1 MC day)** — unchanged. `isStormCooldown(player, worldTime)` is the public helper queried by `ArmorEffects`' `storms_awakening_legs_ready` HUD-badge bonus so badge + gameplay share one source of truth.

Exposes a public helper `hasFullThorSet(PlayerEntity)` that delegates to `ArmorEffects.hasFullSet(player, "thor")` — kept in `ThorEffects` so the file stays self-contained.

Client-only `ClientModInitializer` (registered via `fabric.mod.json` → `entrypoints.client`). Hooks `ItemTooltipCallback.EVENT` to append state-aware lore lines that mirror the server-side gates in `ArmorEffects` — the active/inactive styling truthfully reflects whether the bonus is currently firing. Per-piece MAX_HEALTH values render **natively** in the vanilla attributes block (as "+N Max Health") — no custom tooltip line is needed for those; only status-effect grants and set-membership lines are surfaced here. Also calls `HealthOverlay.register()` (world-space HP bars) and `VersionHandshake.registerClient()` (login-phase version reply).

- `"Part of the Mistborn Set (N/4)"` — gold italic when N=4 and night/Nether/End, silver/base otherwise. Followed by `"Grants Strength"` / `"Grants Speed"` — gold only when 4/4 **and** night/Nether/End.
- `"Part of the Pacifist Set (N/4)"` — gold italic when N=4 (no time gate), silver/base otherwise. Followed by `"Grants Regeneration"` — shares the 4/4 active flag.
- `"Part of the Poseidon Set (N/4)"` — gold italic when N=4 (no time gate), silver/base otherwise. Followed by `"Grants Empowered Riptide"` — gold whenever the full set is worn, since the trident/hand check is gameplay-implicit (you can't riptide without a riptide trident in your hand anyway).
- `"Part of the Fire Serpent Set (N/4)"` — gold italic when N=4 (no time gate), silver/base otherwise. Followed by `"Grants Messmer's Venom"` — gold whenever the full set is worn.
- `"Part of the Knight Radiant Set (N/4)"` — gold italic when N=4 (no time gate), silver/base otherwise. Followed by `"Grants Radiant Might"` — gold whenever the full set is worn.
- `"Part of the Necromancer Set (N/4)"` — gold italic when N=4 (no time gate), silver/base otherwise. Followed by `"Grants Rotten Muscle"` — gold whenever the full set is worn AND the summon is off cooldown. During the cooldown window the line renders in silver with a crimson italic `MM:SS` countdown suffix read off the stack's `NECROMANCER_COOLDOWN_END` component (stamped server-side on every worn piece when the summon fires). Uses the `grantLineWithCooldown(prefix, keyword, active, cooldownTicks)` helper — formats via a ceiling-divide over 20 so the final second rounds up instead of showing `0:00`. The same helper also serves Warrior's Greaves' `"Grants Storm's Awakening"` line — during the 1-MC-day storm cooldown, the cooldown MM:SS is read via the new `FamSpecialClient.stormCooldownRemaining(ItemStack)` helper against the `STORM_COOLDOWN_END` component.
- `"Part of the Esh-Endra-Navesh Set (N/4)"` — gold italic when N=4 (no time gate), silver/base otherwise. Followed by `"Grants Haste"` — gold whenever the full set is worn.
- `"Part of the Shurima Set (N/4)"` — gold italic when N=4 **AND** the player is in a desert-family biome (`DESERT`, `BADLANDS`, or `ERODED_BADLANDS` — gated via `FamSpecialClient.isInDesert()`; name kept for continuity, semantic is "any desert-family biome"), silver/base otherwise. Followed by `"Grants Shuriman Endurance"` — same gate (full set + desert-family biome; the 4/4 bonus still gates on desert). On Drifting Sands (the boots) specifically, `"Grants Sun's Protection"` — gold **whenever the boots are worn** (the prior desert gate was removed when the bonus id was renamed `suns_protection_boots_desert` → `suns_protection_boots`; the tooltip's active state was updated to match). On Sun Disc Spear, `"Grants Emperor's Divide"` — gold when held in the main hand (no time/biome gate). The Shurima-Set and Shuriman-Endurance tooltip branches still read the biome live via the client world.
- `"Grants Night Vision"` — gold when helmet is worn, silver otherwise.
- `"Grants Water Breathing"` — gold when helmet is worn, silver otherwise.
- `"Grants Bad Omen"` — gold when the boots (Dhoom Machale, flagged `GRANTS_OMINOUS`) are worn AND it's night or Nether/End. Shares the Mistborn night gate via `isNightOrTimeless()`.
- `"Grants Messmer's Flame Aegis"` — gold when helmet (Impaler's Crown, flagged `GRANTS_MESSMERS_FLAME`) is worn, silver otherwise.
- `"Grants Healer's Vision"` — gold when helmet (Sage's Crown, flagged `SHOWS_ENTITY_HP`) is worn, silver otherwise.
- `"Grants Nightfall's Might"` — gold when held in the main hand and night/Nether/End. Driven by the `NIGHT_STRENGTH` component on the Atium Dagger — the tooltip keyword is cosmetic-renamed from the component id for theme consistency with the Mistborn palette.
- `"Grants Sage's Grace"` — gold when held in the main hand (no time gate), silver otherwise. Driven by the `HEALS_TARGET` mainhand flag on the Sage's Grace sword; the tooltip keyword was renamed from the older "Pacifist Strike" label to match the new `SAGES_GRACE` cosmetic HUD badge. The sword item name and the `HEALS_TARGET` flag name are unchanged — only the badge/tooltip-keyword is renamed.
- `"Grants Tireless Lunge"` — gold when held in the main hand (no time gate), silver otherwise.
- `"Grants Stormlight"` — gold when any Shard piece (flagged `REGENS_DURABILITY`) is equipped, silver otherwise. Shown on each of the 4 Shard pieces; mirrors the `stormlight_any_piece` server gate.
- `"Grants Shardbearing"` — gold when held in the main hand (no time gate), silver otherwise. Shown on Oathbringer.
- `"Grants Undead Resistance"` — gold when the chestplate (Smooth Criminal's Vestment, flagged `GRANTS_UNDEAD_RESISTANCE`) is worn, silver otherwise. Shown only on the chestplate — mirrors the CHEST-slot server gate in `LivingEntityMixin`'s `famspecial$undeadResistance` handler.
- `"Grants Wither Touch"` — gold when held in the main hand (no time gate), silver otherwise. Shown on Thriller's Edge.
- `"Grants Shadi Buff"` — gold when held in the main hand (no time gate), silver otherwise. Shown on Just Hit Bro — mirrors the `shadi_buff_mainhand` server gate.
- `"Part of the Raider Set (N/4)"` — gold italic when N=4 AND the totem-save cooldown is clear, silver/base otherwise. Shown on each of the 4 Raider's Legacy armor pieces. Followed by **two** grant lines: `"Grants Technoblade Never Dies"` — rendered via `grantLineWithCooldown` (same prior art as Rotten Muscle / Storm's Awakening): gold when 4/4 worn AND `hasEffect(player, TECHNOBLADE_NEVER_DIES)` AND the totem-save cooldown is clear; during the **5-min** cooldown window the line renders off-color with a crimson MM:SS suffix read off the stack's `TECHNOBLADE_TOTEM_COOLDOWN_END` component via the `FamSpecialClient.technobladeCooldownRemaining(ItemStack)` helper (stamped server-side on every worn Raider piece when `TechnobladeSave` fires). AND `"Grants Featherweight"` — no cooldown; gold whenever 4/4 worn AND `hasEffect(player, TECHNOBLADE_FEATHERWEIGHT)`, silver otherwise. On Trousers of the Trail specifically, also `"Grants Bounty Hunter"` — gold whenever the leggings are worn (no time gate; mirrors the `bounty_hunter_legs` server gate). On Fortune & Glory, `"Grants Crusader's Volley"` — gold when held in the main hand (no time gate; mirrors the `crusaders_volley_mainhand` server gate). Fortune & Glory does **not** carry the set line — it's theme-adjacent (no `SET_ID`).
- `"Part of the Thor Set (N/5)"` — Thor is the only **5/5** set in the mod. Mjolnir counts as the 5th piece via the `countThorPieces()` helper, which sums the four Thor armor slots and adds 1 if `THOR_MACE` is in the main hand. Rendered via the `buildSetLine(setName, count, max, active)` overload that accepts an explicit `max` (other sets stay on the 4-arg overload at `max=4`). Shown on all 5 Thor pieces. Followed by `"Grants God of Thunder"` (Mjolnir only) + `"Grants Asgardian's Flight"` on Mjolnir; `"Grants Storm's Awakening"` (Warrior's Greaves only) + `"Grants Asgardian's Flight"` on Warrior's Greaves; `"Grants Asgardian's Flight"` alone on the other three armor pieces. The `"Grants Storm's Awakening"` line is rendered via `grantLineWithCooldown` — during the 1-MC-day storm cooldown, the crimson `MM:SS` countdown suffix is read off the worn Warrior's Greaves' `STORM_COOLDOWN_END` component via the `FamSpecialClient.stormCooldownRemaining(ItemStack)` helper (parallel to the Rotten Muscle cooldown suffix path). The Asgardian's Flight line goes gold only when the full 5/5 is in place — read off the client-side `hasEffect(player, ASGARDIANS_FLIGHT)` helper rather than re-deriving the gate, mirroring the server's `hasStatusEffect` check used by `MjolnirMaceItem`.

All "Grants X" lines share the `grantLine(prefix, keyword, active)` helper; set headings share `buildSetLine(setName, count, active)` (4-arg, defaults `max=4`) or the `buildSetLine(setName, count, max, active)` overload (Thor uses `max=5`). The client-side `hasEffect(player, effect)` helper is the canonical way to read effect presence in tooltips; `countThorPieces()` is the only set whose count needs the helper because Mjolnir-as-5th-piece can't be derived from armor-slot iteration alone.

**Hidden-mechanic convention**: "Grants X" lines never explain *why* they're silver — the gate (night/dimension/main-hand) is deliberately left for the player to discover by watching the color flip. Gold = firing now, silver = not firing; no caveat text.

The `isNightOrTimeless()` helper duplicates the server's dimension/time check so client and server agree on when a bonus is "on".

## `client/HealthOverlay.java`

Client-only world-space HP bar overlay above remote players. Registered on `WorldRenderEvents.AFTER_ENTITIES`; runs every frame but bails immediately if the local player's HEAD slot isn't flagged `SHOWS_ENTITY_HP` (Sage's Crown). When active, iterates `world.getPlayers()` (narrower/cheaper than full entity iteration), skips self + removed/dead/spectator players + anything beyond `VIEW_DISTANCE_SQ` (48²), and draws a two-quad bar (dark bg + HP-colored fg) above each remaining player's head. Billboards via `matrices.multiply(camera.getRotation())`, scale `0.025` (matches vanilla nameplate scale), head clearance `0.55` blocks above the entity. Uses `RenderLayers.debugQuads()` (POSITION_COLOR pipeline) — text-background layers silently drop writes due to a vertex-format mismatch. Three-band color ramp: green ≥ 66%, yellow ≥ 33%, red otherwise. Purely client-side — health is read live off the tracked client entity, no server involvement.

## `net/VersionHandshake.java`

See the "Both-sides install requirement" paragraph at the top of this file. Server registers on `onInitialize`; client registers in `FamSpecialClient.onInitializeClient`.
